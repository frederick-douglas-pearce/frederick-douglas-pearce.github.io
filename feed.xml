<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://frederick-douglas-pearce.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://frederick-douglas-pearce.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-20T14:01:59+00:00</updated><id>https://frederick-douglas-pearce.github.io/feed.xml</id><title type="html">blank</title><subtitle>Frederick Douglas Pearce&apos;s personal website that provides highlights from his career as a data scientist and Earth scientist. </subtitle><entry><title type="html">What launched this turn? Subagents, MCP tools, and Skills</title><link href="https://frederick-douglas-pearce.github.io/blog/2026/what-launched-this-turn/" rel="alternate" type="text/html" title="What launched this turn? Subagents, MCP tools, and Skills"/><published>2026-06-18T08:00:00+00:00</published><updated>2026-06-18T08:00:00+00:00</updated><id>https://frederick-douglas-pearce.github.io/blog/2026/what-launched-this-turn</id><content type="html" xml:base="https://frederick-douglas-pearce.github.io/blog/2026/what-launched-this-turn/"><![CDATA[<p><a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/posts/2026-06-11-inside-the-subagent-trace-file.md">Part 3 of this series</a> opened the subagent trace file and, in passing, named a family of fields it didn’t stop to unpack: <code class="language-plaintext highlighter-rouge">attributionAgent</code>, <code class="language-plaintext highlighter-rouge">attributionMcpServer</code>, <code class="language-plaintext highlighter-rouge">attributionMcpTool</code>, <code class="language-plaintext highlighter-rouge">attributionSkill</code>. It called them markers that tell you that you’ve crossed into a sidechain and moved on. The post’s job was the file split, not provenance.</p> <p>This is the aside that picks them up. It’s short and standalone: one field family, what each member records, and the one trap to look out for.</p> <p>Here’s the whole idea in a sentence. <strong>Every <code class="language-plaintext highlighter-rouge">assistant</code> line can carry a record of how it came to exist</strong> — which subagent it ran as, which MCP tool it routed through, which Skill it ran under — and that record sits at the top level of the line, queryable without ever descending into <code class="language-plaintext highlighter-rouge">message.content</code>.</p> <hr/> <h2 id="provenance-not-content">Provenance, not content</h2> <p>Part 2 made a point of the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/posts/2026-06-04-reading-a-claude-code-session-line-by-line.md"><code class="language-plaintext highlighter-rouge">snake_case</code> / <code class="language-plaintext highlighter-rouge">camelCase</code> split</a>: <code class="language-plaintext highlighter-rouge">message.content</code> is Anthropic API payload (<code class="language-plaintext highlighter-rouge">snake_case</code>), and the fields Claude Code wraps around it are harness bookkeeping (<code class="language-plaintext highlighter-rouge">camelCase</code>). The <code class="language-plaintext highlighter-rouge">attribution*</code> family is squarely the second kind. These fields aren’t part of what the model said. They’re Claude Code’s annotation of how this turn was routed, recorded alongside the message rather than inside it.</p> <p>That’s a question the content can’t answer about itself. A <code class="language-plaintext highlighter-rouge">tool_use</code> block knows it’s calling <code class="language-plaintext highlighter-rouge">mcp__github__get_issue</code>; it doesn’t know it’s doing so as the <code class="language-plaintext highlighter-rouge">pm</code> subagent, three levels removed from your prompt. The attribution fields carry that context at the top level of the line.</p> <p>There are three kinds of provenance here, across four fields:</p> <table> <thead> <tr> <th>Field</th> <th>What it records</th> <th>Example value</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">attributionAgent</code></td> <td>The subagent <em>type</em> this turn ran as</td> <td><code class="language-plaintext highlighter-rouge">"pm"</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">attributionMcpServer</code> / <code class="language-plaintext highlighter-rouge">attributionMcpTool</code></td> <td>The MCP server + tool this turn routed through</td> <td><code class="language-plaintext highlighter-rouge">"github"</code> / <code class="language-plaintext highlighter-rouge">"get_issue"</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">attributionSkill</code></td> <td>The invoked Skill this turn ran under</td> <td>a Skill name</td> </tr> </tbody> </table> <p>Each has a <strong>strict per-line-type presence pattern</strong>, documented in full in <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md#attribution-fields"><code class="language-plaintext highlighter-rouge">reference/subagent-traces.md</code></a>: all four appear on <code class="language-plaintext highlighter-rouge">assistant</code> lines only, never on <code class="language-plaintext highlighter-rouge">user</code> or <code class="language-plaintext highlighter-rouge">attachment</code> lines. So a parser can branch on <code class="language-plaintext highlighter-rouge">type == "assistant"</code> first and only look for these there.</p> <p>The worked examples below run against the synthetic <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-subagent-trace.jsonl"><code class="language-plaintext highlighter-rouge">anatomy-subagent-trace.jsonl</code></a> fixture — a sixteen-line <code class="language-plaintext highlighter-rouge">pm</code> subagent run that reads an issue and posts two comments. It’s valid JSONL, so the <code class="language-plaintext highlighter-rouge">jq</code> snippets execute against it directly.</p> <hr/> <h2 id="attributionagent--which-agent-ran"><code class="language-plaintext highlighter-rouge">attributionAgent</code> — which agent ran</h2> <p>This is the one most people reach for first, and the most reliable. Every <code class="language-plaintext highlighter-rouge">assistant</code> line inside a subagent trace carries <code class="language-plaintext highlighter-rouge">attributionAgent</code>, set to the subagent type that ran — <code class="language-plaintext highlighter-rouge">"pm"</code>, <code class="language-plaintext highlighter-rouge">"general-purpose"</code>, a custom agent name. The value is the same string the parent recorded as <code class="language-plaintext highlighter-rouge">toolUseResult.agentType</code>, and the same one you passed as <code class="language-plaintext highlighter-rouge">subagent_type</code> in the <code class="language-plaintext highlighter-rouge">Agent</code> call. One run, one agent type, stamped on every model turn it took.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Which agent produced each assistant turn?</span>
jq <span class="nt">-r</span> <span class="s1">'select(.type == "assistant") | .attributionAgent // "(main loop)"'</span> <span class="se">\</span>
  anatomy-subagent-trace.jsonl | <span class="nb">sort</span> | <span class="nb">uniq</span> <span class="nt">-c</span>
<span class="c">#   8 pm</span>
</code></pre></div></div> <p>All eight <code class="language-plaintext highlighter-rouge">assistant</code> lines come back <code class="language-plaintext highlighter-rouge">pm</code> — the fixture is a single <code class="language-plaintext highlighter-rouge">pm</code> invocation. The <code class="language-plaintext highlighter-rouge">// "(main loop)"</code> fallback is doing nothing here, but it matters the moment you point this at a parent session: there, <code class="language-plaintext highlighter-rouge">attributionAgent</code> is <strong>absent</strong>, and every main-loop turn falls through to <code class="language-plaintext highlighter-rouge">(main loop)</code>. That contrast is the field’s whole value — it partitions turns by which agent produced them, with the main loop as the implicit “no agent” bucket.</p> <p>One caveat to carry into the next section: the observational scan behind the reference doc found <code class="language-plaintext highlighter-rouge">attributionAgent</code> on sidechain assistant lines <strong>only</strong> (10,291 sidechain, 0 main). So in practice its presence <em>does</em> track subagent-ness. That’s a property of how it’s emitted, though — not a license to use it as your subagent test. More on why in a moment.</p> <hr/> <h2 id="attributionmcpserver--attributionmcptool--which-mcp-tool"><code class="language-plaintext highlighter-rouge">attributionMcpServer</code> / <code class="language-plaintext highlighter-rouge">attributionMcpTool</code> — which MCP tool</h2> <p>These two travel together — present as a pair or absent as a pair — and they appear only on <code class="language-plaintext highlighter-rouge">assistant</code> turns that interact with an <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md#when-attributionmcpserverattributionmcptool-appear">MCP</a>-defined tool. Our <code class="language-plaintext highlighter-rouge">pm</code> run calls <code class="language-plaintext highlighter-rouge">mcp__github__get_issue</code> once and <code class="language-plaintext highlighter-rouge">mcp__github__add_issue_comment</code> twice — three MCP turns out of eight — and exactly those three lines carry the pair:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Which MCP server/tool did the MCP turns route through?</span>
jq <span class="nt">-r</span> <span class="s1">'select(.type == "assistant" and has("attributionMcpServer"))
  | "\(.attributionMcpServer)__\(.attributionMcpTool)"'</span> <span class="se">\</span>
  anatomy-subagent-trace.jsonl | <span class="nb">sort</span> | <span class="nb">uniq</span> <span class="nt">-c</span>
<span class="c">#   2 github__add_issue_comment</span>
<span class="c">#   1 github__get_issue</span>
</code></pre></div></div> <p>The other five <code class="language-plaintext highlighter-rouge">assistant</code> lines — the <code class="language-plaintext highlighter-rouge">Read</code>s and the final summary — <strong>omit both keys entirely</strong>. They aren’t set to empty strings; the keys simply aren’t on the line. So <code class="language-plaintext highlighter-rouge">has("attributionMcpServer")</code> is the right test, not a truthiness check on the value.</p> <p>What the pair gives you that the <code class="language-plaintext highlighter-rouge">tool_use</code> block doesn’t is a <em>decoded</em> form, pre-split at the top level. The call itself carries <code class="language-plaintext highlighter-rouge">name: "mcp__github__get_issue"</code> buried in <code class="language-plaintext highlighter-rouge">message.content</code>; the attribution pair lifts the <code class="language-plaintext highlighter-rouge">github</code> server and the <code class="language-plaintext highlighter-rouge">get_issue</code> tool out to the line’s surface, so you can group turns by server or by tool without parsing the <code class="language-plaintext highlighter-rouge">__</code>-delimited name or walking into the content array.</p> <p>One honest gap: the <strong>exact</strong> trigger condition is <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md#open-verification-items">still being verified</a>. Whether the pair lands on the assistant line that <em>emits</em> the MCP <code class="language-plaintext highlighter-rouge">tool_use</code>, only on the turn that <em>follows</em> an MCP <code class="language-plaintext highlighter-rouge">tool_result</code>, or on both, isn’t yet disambiguated — roughly half the assistant lines in a given MCP flow carry it. Treat it as “MCP-routing context is present when this turn touches an MCP server,” and don’t assume one-to-one with <code class="language-plaintext highlighter-rouge">tool_use</code> emission until that’s nailed down.</p> <hr/> <h2 id="attributionskill--and-the-trap"><code class="language-plaintext highlighter-rouge">attributionSkill</code> — and the trap</h2> <p>The fourth field is the same shape as the others — present on <code class="language-plaintext highlighter-rouge">assistant</code> lines, naming the invoked Skill a turn ran under — with one difference that changes how you can use it.</p> <p><strong><code class="language-plaintext highlighter-rouge">attributionSkill</code> is not a sidechain signal.</strong> Skills run in the main loop, not just inside subagents, so the field shows up in <em>parent</em> sessions too. The scan behind the reference doc found it on both sides of the line: 4,785 sidechain assistant lines and 1,895 main-session ones. Compare that to <code class="language-plaintext highlighter-rouge">attributionAgent</code>’s clean 10,291 / 0 split, and the difference is the whole point — <code class="language-plaintext highlighter-rouge">attributionAgent</code> happens to be subagent-exclusive; <code class="language-plaintext highlighter-rouge">attributionSkill</code> is not.</p> <p>Here’s the trap that sits underneath this, and it’s worth stating plainly because it’s an easy reflex:</p> <blockquote> <p>Don’t decide “is this a subagent line?” by checking whether an <code class="language-plaintext highlighter-rouge">attribution*</code> field is present.</p> </blockquote> <p>It seems reasonable — the fields cluster in trace files, so “has an attribution field” feels like “is a subagent line.” But it’s wrong two ways. <code class="language-plaintext highlighter-rouge">attributionSkill</code> appears on main-loop turns whenever a Skill is active, and the <code class="language-plaintext highlighter-rouge">attributionMcp*</code> pair appears on main-loop turns whenever an MCP tool is used outside a subagent. Branch on either and you’ll tag ordinary main-session turns as subagent work.</p> <p>The field that actually answers the question is <code class="language-plaintext highlighter-rouge">isSidechain</code> — the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md#the-issidechain-marker">canonical, content-free signal</a> Part 3 built its case around. The division of labor is clean once you see it: <strong>the attribution fields tell you <em>how</em> a turn was launched; <code class="language-plaintext highlighter-rouge">isSidechain</code> tells you <em>where</em> it lives.</strong> Provenance and location are different questions, and only one of them is a subagent test.</p> <hr/> <h2 id="what-provenance-buys-you">What provenance buys you</h2> <p>Because all of this sits at the top level of the line, you can attribute behavior — and cost — by origin without ever reparsing <code class="language-plaintext highlighter-rouge">message.content</code>. Each of these is a single top-level field filter:</p> <ul> <li><strong>Per agent.</strong> Group <code class="language-plaintext highlighter-rouge">assistant</code> turns (or their <code class="language-plaintext highlighter-rouge">message.usage</code>) by <code class="language-plaintext highlighter-rouge">attributionAgent</code> to see which subagent type ran up the most turns or tokens. Part 4 is where the token side of this gets its full treatment — but the <em>grouping key</em> is this field.</li> <li><strong>Per MCP tool.</strong> Tally <code class="language-plaintext highlighter-rouge">attributionMcpServer</code> / <code class="language-plaintext highlighter-rouge">attributionMcpTool</code> to see which servers and tools a session leans on, and which subagents lean on them.</li> <li><strong>Per Skill.</strong> Filter on <code class="language-plaintext highlighter-rouge">attributionSkill</code> to find every turn a Skill touched — across both the main loop and subagents, which is exactly the reach that makes it <em>not</em> a sidechain signal but <em>does</em> make it a complete Skill-usage view.</li> </ul> <p>None of these needs the content array. That’s the practical payoff of provenance living in the harness bookkeeping layer rather than inside the model’s message.</p> <hr/> <h2 id="try-it-on-your-own-sessions">Try it on your own sessions</h2> <p>Point the agent grouping at a real session and its traces together. Main-loop turns fall into <code class="language-plaintext highlighter-rouge">(main loop)</code>; subagent turns report their agent type:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Which agent produced each assistant turn, across a session AND its traces:</span>
jq <span class="nt">-r</span> <span class="s1">'select(.type == "assistant") | .attributionAgent // "(main loop)"'</span> <span class="se">\</span>
  ~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;.jsonl <span class="se">\</span>
  ~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;/subagents/<span class="k">*</span>.jsonl <span class="se">\</span>
  | <span class="nb">sort</span> | <span class="nb">uniq</span> <span class="nt">-c</span>
</code></pre></div></div> <p>If you’ve run anything under a Skill recently, this next one makes the “not a sidechain signal” point concrete on your own data — you’ll see <code class="language-plaintext highlighter-rouge">attributionSkill</code> values coming from the parent session file, not just the trace files:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Every Skill-attributed turn, with which file it came from:</span>
<span class="k">for </span>f <span class="k">in</span> ~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;.jsonl <span class="se">\</span>
         ~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;/subagents/<span class="k">*</span>.jsonl<span class="p">;</span> <span class="k">do
  </span>jq <span class="nt">-r</span> <span class="nt">--arg</span> f <span class="s2">"</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span><span class="si">)</span><span class="s2">"</span> <span class="se">\</span>
    <span class="s1">'select(.type == "assistant" and has("attributionSkill"))
     | "\($f)\t\(.attributionSkill)"'</span> <span class="s2">"</span><span class="nv">$f</span><span class="s2">"</span>
<span class="k">done</span>
</code></pre></div></div> <p>Both use defensive <code class="language-plaintext highlighter-rouge">?</code>-free <code class="language-plaintext highlighter-rouge">select</code> guards on <code class="language-plaintext highlighter-rouge">type</code>, so non-matching lines are skipped rather than raising — the same discipline from Part 2’s snippets. They assume <code class="language-plaintext highlighter-rouge">jq</code> on bash or zsh.</p> <hr/> <h2 id="whats-next">What’s next</h2> <p>This aside doesn’t change the through-line: Part 3 still hands off to <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/.claude/specs/series-outline.md">Part 4 — “Token accounting is harder than it looks”</a>. If anything it sets the table for it. Once you can compute what a session cost, the natural next question is <em>whose</em> cost — which agent, which tool, which Skill ran up the bill. The attribution fields are the grouping keys for that slice; Part 4 is the arithmetic.</p> <p>This is verified against <strong>Claude Code v2.1.150</strong>, the same baseline as Part 3. The attribution family is exactly the kind of harness bookkeeping that evolves independently of the API message format, so if your own sessions show an attribution field — or a trigger condition — I haven’t described, especially from the Agent SDK, that’s the kind of thing that updates the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md">reference doc</a> and sharpens later posts. Please open an issue in the repo.</p> <p>The sources behind this post:</p> <ul> <li><strong>Reference grounding:</strong> <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md#attribution-fields"><code class="language-plaintext highlighter-rouge">reference/subagent-traces.md</code> § Attribution fields</a></li> <li><strong>Synthetic fixture</strong> — valid JSONL, so the <code class="language-plaintext highlighter-rouge">jq</code> snippets run against it directly: the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-subagent-trace.jsonl">subagent trace</a></li> </ul> <hr/> <p><em>Drafted with Claude Code (verified against v2.1.150). The ideas, claims, and any errors are mine.</em></p>]]></content><author><name></name></author><category term="foundation"/><category term="claude-code"/><category term="agent-sdk"/><category term="jsonl"/><category term="sessions"/><category term="subagents"/><summary type="html"><![CDATA[A short aside off Part 3. Every assistant line can carry a record of how it came to exist — which subagent ran it, which MCP tool it routed through, which Skill it ran under. The attribution* fields, what each one records, and the one that is not the sidechain signal it looks like.]]></summary></entry><entry><title type="html">Inside the subagent trace file</title><link href="https://frederick-douglas-pearce.github.io/blog/2026/inside-the-subagent-trace-file/" rel="alternate" type="text/html" title="Inside the subagent trace file"/><published>2026-06-11T08:00:00+00:00</published><updated>2026-06-11T08:00:00+00:00</updated><id>https://frederick-douglas-pearce.github.io/blog/2026/inside-the-subagent-trace-file</id><content type="html" xml:base="https://frederick-douglas-pearce.github.io/blog/2026/inside-the-subagent-trace-file/"><![CDATA[<p><a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/posts/2026-06-04-reading-a-claude-code-session-line-by-line.md">Part 2 of this series</a> ended by teasing the value of <code class="language-plaintext highlighter-rouge">agent_id</code> in the parent session jsonl data as “the literal handle that takes you to the subagent’s full trace file”.</p> <p>This post fills in the details. We’re going to take that <code class="language-plaintext highlighter-rouge">agent_id</code>, follow it to a separate file on disk, and open it. The same activity is recorded twice — at two granularities, in two files — and that split is the thread this post pulls on.</p> <p>Here’s the short version: when the parent session delegates to a subagent, it records a <strong>rollup</strong> — a condensed account of the whole run: a duration, a token total, a per-tool count, and the subagent’s final summary. It does not record what the subagent actually did, step by step. That lives somewhere else.</p> <h2 id="following-the-handle">Following the handle</h2> <p>The <code class="language-plaintext highlighter-rouge">agent_id</code> from the Part 2 snippet is <code class="language-plaintext highlighter-rouge">99999999-9999-9999-9999-999999999001</code>. That same UUID is a file name. The subagent’s complete trace lives at:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;/subagents/agent-99999999-9999-9999-9999-999999999001.jsonl
</code></pre></div></div> <p>Three things to notice about that path:</p> <ul> <li><code class="language-plaintext highlighter-rouge">&lt;slug&gt;</code> is the same project root as the parent session — the slugified <code class="language-plaintext highlighter-rouge">cwd</code> (Part 1 covered the slug convention).</li> <li><code class="language-plaintext highlighter-rouge">&lt;session-uuid&gt;/</code> is a directory that sits <em>next to</em> the parent’s <code class="language-plaintext highlighter-rouge">&lt;session-uuid&gt;.jsonl</code> file, sharing its name. It’s created lazily — it exists only when the session produces overflow that doesn’t belong inline: a subagent trace, a spilled tool result, or both.</li> <li><code class="language-plaintext highlighter-rouge">subagents/</code> holds the traces — one <code class="language-plaintext highlighter-rouge">agent-&lt;agentId&gt;.jsonl</code> per subagent invocation, each with its own <code class="language-plaintext highlighter-rouge">agentId</code>. Invoke the <code class="language-plaintext highlighter-rouge">pm</code> agent three times in one session and you get three traces. Each trace also gets a small <code class="language-plaintext highlighter-rouge">agent-&lt;agentId&gt;.meta.json</code> companion, and there’s often a <code class="language-plaintext highlighter-rouge">tool-results/</code> sibling directory too — both covered below.</li> </ul> <p>So the directory layout for a session that delegated work looks like this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.claude/projects/&lt;slug&gt;/
├── &lt;session-uuid&gt;.jsonl                          # parent session
└── &lt;session-uuid&gt;/
    ├── subagents/
    │   ├── agent-&lt;agentId&gt;.jsonl                  # the trace — one per invocation
    │   ├── agent-&lt;agentId&gt;.meta.json             #   + a tiny manifest beside each trace
    │   └── …                                     #   (one .jsonl + .meta.json pair per subagent)
    └── tool-results/                             # large tool outputs spilled out of the JSONL (see Part 5)
        └── …
</code></pre></div></div> <p>The only parent → subagent link in the session data is <code class="language-plaintext highlighter-rouge">toolUseResult.agentId</code> on the parent’s <code class="language-plaintext highlighter-rouge">user</code> line — the same value the Part 2 snippet surfaced. The file name is that value with an <code class="language-plaintext highlighter-rouge">agent-</code> prefix and a <code class="language-plaintext highlighter-rouge">.jsonl</code> suffix. There’s no per-line back-pointer <em>inside</em> the subagent’s lines that names the parent; that direction is reconstructed from where the file sits on disk — with a partial assist from the <code class="language-plaintext highlighter-rouge">meta.json</code> sidecar (below), which records the parent <code class="language-plaintext highlighter-rouge">toolUseId</code> that spawned the run.</p> <p>Two other things share that <code class="language-plaintext highlighter-rouge">&lt;session-uuid&gt;/</code> directory, and naming them now keeps the layout above from being a surprise when you open your own:</p> <ul> <li><strong>A manifest beside each trace.</strong> Every <code class="language-plaintext highlighter-rouge">agent-&lt;agentId&gt;.jsonl</code> has a small <code class="language-plaintext highlighter-rouge">agent-&lt;agentId&gt;.meta.json</code> companion — a few hundred bytes. It’s an index card for the run: the subagent’s <code class="language-plaintext highlighter-rouge">agentType</code>, the human-readable <code class="language-plaintext highlighter-rouge">description</code> from the parent’s <code class="language-plaintext highlighter-rouge">Agent</code> call, the <code class="language-plaintext highlighter-rouge">toolUseId</code> that spawned it, and a <code class="language-plaintext highlighter-rouge">worktreePath</code> when the subagent ran in an isolated git worktree. That <code class="language-plaintext highlighter-rouge">toolUseId</code> is the one piece of the subagent → parent relationship carried in data rather than reconstructed from disk: it names the exact parent <code class="language-plaintext highlighter-rouge">Agent</code> <code class="language-plaintext highlighter-rouge">tool_use</code> this run answers, which the file’s location alone can’t tell you. The manifest also lets Claude Code — and your own tooling — list and route subagents without opening and parsing every trace. One wrinkle worth flagging: the manifest spells the key <code class="language-plaintext highlighter-rouge">toolUseId</code>, while the session lines use <code class="language-plaintext highlighter-rouge">toolUseID</code>. Same value, different casing, in files that sit next to each other.</li> <li><strong>A <code class="language-plaintext highlighter-rouge">tool-results/</code> sibling.</strong> Alongside <code class="language-plaintext highlighter-rouge">subagents/</code> you’ll often see a <code class="language-plaintext highlighter-rouge">tool-results/</code> directory. It isn’t subagent-specific — it’s where Claude Code spills <em>large</em> tool outputs that would otherwise bloat a single JSONL line. When a tool result is big, the <code class="language-plaintext highlighter-rouge">tool_result</code> content in the session carries a <code class="language-plaintext highlighter-rouge">&lt;persisted-output&gt;</code> wrapper with a truncated preview, and the full payload lands in <code class="language-plaintext highlighter-rouge">tool-results/</code>, named after the tool call that produced it. That’s a tool-invocation concern more than a subagent one, so it gets its full treatment in Part 5 — it’s flagged here only so the directory listing makes sense.</li> </ul> <p>A word on versions: this post is verified against Claude Code v2.1.150, and the trace-file internals below hold there. The two sidecars are a reminder that the on-disk <em>layout</em> keeps evolving independently of the line format — <code class="language-plaintext highlighter-rouge">tool-results/</code> predates this post’s baseline, and the per-subagent <code class="language-plaintext highlighter-rouge">meta.json</code> shows up in current sessions. The <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md">reference docs</a> track the directory layout as it shifts; the rest of this post stays inside the trace file itself.</p> <h2 id="whats-actually-in-the-file">What’s actually in the file</h2> <p>Open <code class="language-plaintext highlighter-rouge">agent-99999999-...-999999999001.jsonl</code> and you’ll find it looks <em>almost</em> exactly like a parent session: the same line shape, the same <code class="language-plaintext highlighter-rouge">type</code> values, <code class="language-plaintext highlighter-rouge">assistant</code> lines with <code class="language-plaintext highlighter-rouge">message.content</code> and <code class="language-plaintext highlighter-rouge">message.usage</code>, <code class="language-plaintext highlighter-rouge">user</code> lines carrying tool results. If you’ve read Part 2, you can already parse it.</p> <p>Here’s a four-line slice of that trace — the prompt, the first tool call, and the final summary, with the four <code class="language-plaintext highlighter-rouge">Read</code>s and two <code class="language-plaintext highlighter-rouge">add_issue_comment</code>s in between elided. The full sixteen-line run pairs with the parent invocation <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-subagent-trace.jsonl">in the fixtures</a>:</p> <pre><code class="language-jsonl">{"type":"user","sessionId":"77777777-7777-7777-7777-777777777003","uuid":"bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbb0001","parentUuid":null,"isSidechain":true,"agentId":"99999999-9999-9999-9999-999999999001","promptId":"prompt_synthetic_001","cwd":"/home/dev/example-project","version":"2.1.150","timestamp":"2026-05-22T16:45:02.300Z","message":{"role":"user","content":"Read issue #5 and draft acceptance criteria for each open item."}}
{"type":"assistant","sessionId":"77777777-7777-7777-7777-777777777003","uuid":"aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaa0001","parentUuid":"bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbb0001","isSidechain":true,"agentId":"99999999-9999-9999-9999-999999999001","attributionAgent":"pm","attributionMcpServer":"github","attributionMcpTool":"get_issue","cwd":"/home/dev/example-project","version":"2.1.150","timestamp":"2026-05-22T16:45:03.110Z","message":{"id":"msg_synthetic_sub_001","type":"message","role":"assistant","model":"claude-sonnet-4-6","content":[{"type":"text","text":"I'll read issue #5 first."},{"type":"tool_use","id":"toolu_synthetic_sub_001","name":"mcp__github__get_issue","input":{"owner":"example","repo":"example","issue_number":5}}],"usage":{"input_tokens":3,"output_tokens":60,"cache_creation_input_tokens":13000,"cache_read_input_tokens":0},"stop_reason":"tool_use"}}
{"type":"user","sessionId":"77777777-7777-7777-7777-777777777003","uuid":"bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbb0002","parentUuid":"aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaa0001","isSidechain":true,"agentId":"99999999-9999-9999-9999-999999999001","promptId":"prompt_synthetic_001","sourceToolAssistantUUID":"aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaa0001","cwd":"/home/dev/example-project","version":"2.1.150","timestamp":"2026-05-22T16:45:04.512Z","message":{"role":"user","content":[{"type":"tool_result","tool_use_id":"toolu_synthetic_sub_001","content":"Issue #5: Migrate JSONL format docs to reference/. Open items: data-dictionary, subagent-traces, tool-invocation, two coordinating PRs."}]},"toolUseResult":{"status":"success","durationMs":1402}}
{"type":"assistant","sessionId":"77777777-7777-7777-7777-777777777003","uuid":"aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaa0008","parentUuid":"bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbb0008","isSidechain":true,"agentId":"99999999-9999-9999-9999-999999999001","attributionAgent":"pm","cwd":"/home/dev/example-project","version":"2.1.150","timestamp":"2026-05-22T16:47:13.804Z","message":{"id":"msg_synthetic_sub_008","type":"message","role":"assistant","model":"claude-sonnet-4-6","content":[{"type":"text","text":"Drafted acceptance criteria for issue #5 covering scope, success conditions, and test plan. See comment on the issue."}],"usage":{"input_tokens":3,"output_tokens":300,"cache_creation_input_tokens":1500,"cache_read_input_tokens":27000},"stop_reason":"end_turn"}}
</code></pre> <p>Read top to bottom, that’s the shape of a subagent run: the prompt that started it, an <code class="language-plaintext highlighter-rouge">assistant</code> turn that calls a tool, the <code class="language-plaintext highlighter-rouge">tool_result</code> for that call, and — skipping the run’s middle — the final <code class="language-plaintext highlighter-rouge">assistant</code> turn with the summary. (That last line’s <code class="language-plaintext highlighter-rouge">parentUuid</code> points at <code class="language-plaintext highlighter-rouge">…bbbb0008</code>, the result of the second comment, which is one of the elided turns; in the full trace the chain is unbroken.) The summary on that last line — <em>“Drafted acceptance criteria for issue #5…“</em> — is the exact string the parent recorded as the subagent’s <code class="language-plaintext highlighter-rouge">tool_result</code>. Same words, two files.</p> <p>But look at the fields that <em>aren’t</em> in a parent session. Every single line carries <code class="language-plaintext highlighter-rouge">isSidechain: true</code>. Every line carries a top-level <code class="language-plaintext highlighter-rouge">agentId</code>. The <code class="language-plaintext highlighter-rouge">assistant</code> lines carry <code class="language-plaintext highlighter-rouge">attributionAgent</code>. The <code class="language-plaintext highlighter-rouge">user</code> lines carry <code class="language-plaintext highlighter-rouge">promptId</code>, and the tool-result line carries <code class="language-plaintext highlighter-rouge">sourceToolAssistantUUID</code>. None of those appear in a parent session file. They’re the markers that tell you you’ve crossed into a sidechain.</p> <h2 id="issidechain-is-the-field-that-matters"><code class="language-plaintext highlighter-rouge">isSidechain</code> is the field that matters</h2> <p>If you take one field away from this post, take this one.</p> <p>Every line inside a subagent trace file carries <code class="language-plaintext highlighter-rouge">isSidechain: true</code>. Parent-session lines carry <code class="language-plaintext highlighter-rouge">isSidechain: false</code>, or omit the field entirely on some message types. That makes it the <strong>canonical, content-free signal</strong> for telling a subagent line apart from a parent-session line.</p> <p>Why does “content-free” matter? Because the alternatives are all worse:</p> <ul> <li><strong>Don’t branch on file path.</strong> A hook script, or a tool that’s been handed a stream of lines from a pipe, may not know which file a line came from.</li> <li><strong>Don’t branch on the presence of <code class="language-plaintext highlighter-rouge">agentId</code>.</strong> It’s reliable inside trace files, but keying on “does this field exist” is brittle — it couples your parser to one field’s presence rather than to a boolean that exists precisely to answer this question.</li> <li><strong>Don’t branch on <code class="language-plaintext highlighter-rouge">attribution*</code> heuristics.</strong> Those fields have a per-line-type presence pattern (more on that below), so “does this line have <code class="language-plaintext highlighter-rouge">attributionAgent</code>” gives you the wrong answer on every <code class="language-plaintext highlighter-rouge">user</code> line in the trace.</li> </ul> <p><code class="language-plaintext highlighter-rouge">isSidechain</code> is the one field designed to answer “is this a subagent line?” — so branch on it. The operational rule of thumb:</p> <ul> <li><strong>Skip sidechain lines</strong> when you’re counting user-visible turns or rendering the main conversation thread. They aren’t main-conversation turns.</li> <li><strong>Skip them</strong> when aggregating per-session tokens <em>if you’re also reading the parent’s rollup</em> — otherwise you count the same tokens twice (the <a href="#one-gotcha-before-part-4">token-accounting</a> section below is the warning; Part 4 is the fix).</li> <li><strong>Include them</strong> when you’re auditing what the agent actually did — which tools it called, what it reasoned about, how long each step took. That’s the entire reason the trace file exists.</li> </ul> <h2 id="the-sessionid-is-not-the-one-youd-expect">The <code class="language-plaintext highlighter-rouge">sessionId</code> is not the one you’d expect</h2> <p>Here’s the tricky part of the parent ↔ subagent relationship: <strong>the subagent does not share the parent’s <code class="language-plaintext highlighter-rouge">sessionId</code>.</strong></p> <p>Look at the trace fixture again. Every line carries <code class="language-plaintext highlighter-rouge">"sessionId":"77777777-7777-7777-7777-777777777003"</code>. Now look at the parent invocation <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-agent-invocation.jsonl">in the paired fixture</a> — its lines carry <code class="language-plaintext highlighter-rouge">"sessionId":"00000000-0000-0000-0000-000000000003"</code>. Different values. Each subagent invocation gets its <strong>own</strong> <code class="language-plaintext highlighter-rouge">sessionId</code>, distinct from the parent’s and distinct from every other subagent’s.</p> <p>So where is the parent’s session UUID in the subagent file? It isn’t — not in any field. It appears only as the <strong>directory name</strong> that contains the subagent file:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.claude/projects/&lt;slug&gt;/&lt;PARENT-session-uuid&gt;/subagents/agent-&lt;agentId&gt;.jsonl
                          ▲
                          this, and only this, is the parent's sessionId
</code></pre></div></div> <p>The link from subagent back to parent is encoded entirely in the file’s location on disk. There is no per-line back-pointer that names the parent session or the parent’s invoking <code class="language-plaintext highlighter-rouge">assistant</code> line. If you move a subagent file out of its directory, you’ve severed the only thread connecting it to its parent.</p> <p>This has a concrete consequence for tooling: if you glob <code class="language-plaintext highlighter-rouge">~/.claude/projects/**/subagents/*.jsonl</code> and read the lines without tracking which directory each came from, you’ve thrown away the parent linkage. The <code class="language-plaintext highlighter-rouge">agentId</code> tells you <em>which invocation</em> a line belongs to; it does not tell you <em>which parent session</em> launched it. Only the path does.</p> <h2 id="the-subagent-specific-fields-and-the-one-that-lies-about-its-name">The subagent-specific fields, and the one that lies about its name</h2> <p>Three subagent-specific fields show up alongside <code class="language-plaintext highlighter-rouge">agentId</code> — one of them an <code class="language-plaintext highlighter-rouge">attribution*</code> field, two not — and each has a <strong>strict per-line-type presence pattern</strong>. They don’t appear on every line — they appear on specific line types, consistently. Knowing the pattern lets you branch on <code class="language-plaintext highlighter-rouge">type</code> first and only check the fields that apply:</p> <table> <thead> <tr> <th>Field</th> <th><code class="language-plaintext highlighter-rouge">assistant</code> lines</th> <th><code class="language-plaintext highlighter-rouge">user</code> lines</th> <th>What it is</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">agentId</code></td> <td>✅ always</td> <td>✅ always</td> <td>UUID of this subagent invocation — matches the file name and the parent’s <code class="language-plaintext highlighter-rouge">toolUseResult.agentId</code>.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">attributionAgent</code></td> <td>✅ always</td> <td>❌ never</td> <td>The subagent <em>type</em> that ran (<code class="language-plaintext highlighter-rouge">"pm"</code>, <code class="language-plaintext highlighter-rouge">"general-purpose"</code>, a custom agent name). Matches <code class="language-plaintext highlighter-rouge">toolUseResult.agentType</code> on the parent.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">promptId</code></td> <td>❌ never</td> <td>✅ always</td> <td>Identifier for the prompt-flow this user line belongs to. Stable across the user lines of one invocation.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">sourceToolAssistantUUID</code></td> <td>❌ never</td> <td>✅ on tool-result lines only</td> <td>The <code class="language-plaintext highlighter-rouge">uuid</code> of the <code class="language-plaintext highlighter-rouge">assistant</code> line whose <code class="language-plaintext highlighter-rouge">tool_use</code> this user line is the result for.</td> </tr> </tbody> </table> <p>(There’s also an <code class="language-plaintext highlighter-rouge">attributionMcpServer</code> / <code class="language-plaintext highlighter-rouge">attributionMcpTool</code> pair that shows up on <code class="language-plaintext highlighter-rouge">assistant</code> lines involved in an MCP-tool turn — you can see it on the first <code class="language-plaintext highlighter-rouge">assistant</code> line of the fixture, which calls <code class="language-plaintext highlighter-rouge">mcp__github__get_issue</code>. The exact trigger condition is still an <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md#open-verification-items">open verification item</a>.)</p> <p>(And one more, <code class="language-plaintext highlighter-rouge">attributionSkill</code>, marks an <code class="language-plaintext highlighter-rouge">assistant</code> turn that ran under an invoked Skill. It’s the odd one out: unlike the fields above, it shows up in <em>parent</em> sessions too — Skills run in the main loop, not just inside subagents — so it is <em>not</em> a sidechain signal. For the “is this a subagent line?” question, <code class="language-plaintext highlighter-rouge">isSidechain</code> remains the field to branch on; the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md#attribution-fields">reference doc</a> covers <code class="language-plaintext highlighter-rouge">attributionSkill</code> in full.)</p> <p>The interesting one is <code class="language-plaintext highlighter-rouge">sourceToolAssistantUUID</code>, because its name invites a wrong inference. It <em>sounds</em> like it should point back at the parent’s invoking <code class="language-plaintext highlighter-rouge">assistant</code> line — the “source tool” that launched the subagent. It does not.</p> <p>Trace it in the fixture. The tool-result <code class="language-plaintext highlighter-rouge">user</code> line carries <code class="language-plaintext highlighter-rouge">"sourceToolAssistantUUID":"aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaa0001"</code>. That UUID is the <code class="language-plaintext highlighter-rouge">uuid</code> of the <code class="language-plaintext highlighter-rouge">assistant</code> line <em>earlier in the same subagent file</em> — the one that emitted <code class="language-plaintext highlighter-rouge">tool_use</code> id <code class="language-plaintext highlighter-rouge">toolu_synthetic_sub_001</code>. In other words, it’s an <strong>internal pairing key</strong>: it ties a tool result back to the tool call it answers, within the subagent’s own trace.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>subagent file (internal pairing — stays within the file)
─────────────
assistant line:  uuid "aaaa…0001"  emits tool_use "toolu_synthetic_sub_001"
                 ▲
                 │  sourceToolAssistantUUID
                 │
user line:       tool_result.tool_use_id "toolu_synthetic_sub_001"
</code></pre></div></div> <p>It’s redundant with <code class="language-plaintext highlighter-rouge">tool_use_id</code>, but it lives at the top level of the line, which lets a parser reconstruct the assistant ↔ user tool cycle without descending into the <code class="language-plaintext highlighter-rouge">message.content</code> array. Useful — but it points <em>inward</em>, never at the parent. The only parent link carried on the trace lines themselves is the forward <code class="language-plaintext highlighter-rouge">agentId</code>; the back-reference lives in the directory path and the <code class="language-plaintext highlighter-rouge">meta.json</code> sidecar (above), not in the line data.</p> <h2 id="the-rollup-is-a-summary-the-trace-file-is-the-evidence">The rollup is a summary. The trace file is the evidence.</h2> <p>This is the single most important thing to understand about the split, so it’s worth stating plainly: <strong>the parent’s <code class="language-plaintext highlighter-rouge">toolUseResult</code> envelope is a rollup; the subagent trace file is the actual record.</strong></p> <p>Here’s what the parent saw for our <code class="language-plaintext highlighter-rouge">pm</code> invocation — the <code class="language-plaintext highlighter-rouge">toolUseResult</code> from the parent fixture, expanded:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"status"</span><span class="p">:</span><span class="w"> </span><span class="s2">"success"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"prompt"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Read issue #5 and draft acceptance criteria for each open item."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"agentId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"99999999-9999-9999-9999-999999999001"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"agentType"</span><span class="p">:</span><span class="w"> </span><span class="s2">"pm"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"totalDurationMs"</span><span class="p">:</span><span class="w"> </span><span class="mi">132140</span><span class="p">,</span><span class="w">
  </span><span class="nl">"totalTokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">180020</span><span class="p">,</span><span class="w">
  </span><span class="nl">"totalToolUseCount"</span><span class="p">:</span><span class="w"> </span><span class="mi">7</span><span class="p">,</span><span class="w">
  </span><span class="nl">"usage"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"input_tokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">20</span><span class="p">,</span><span class="w">
    </span><span class="nl">"output_tokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">1000</span><span class="p">,</span><span class="w">
    </span><span class="nl">"cache_creation_input_tokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">29000</span><span class="p">,</span><span class="w">
    </span><span class="nl">"cache_read_input_tokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">150000</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"toolStats"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"Read"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w">
    </span><span class="nl">"mcp__github__get_issue"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w">
    </span><span class="nl">"mcp__github__add_issue_comment"</span><span class="p">:</span><span class="w"> </span><span class="mi">2</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>That’s genuinely useful — at a glance you know the <code class="language-plaintext highlighter-rouge">pm</code> agent ran for ~132 seconds, burned ~180K tokens (nearly all cache reads), and made 7 tool calls across <code class="language-plaintext highlighter-rouge">Read</code>, <code class="language-plaintext highlighter-rouge">get_issue</code>, and <code class="language-plaintext highlighter-rouge">add_issue_comment</code>. For a lot of questions, that’s enough, and you never have to open the trace file.</p> <p>But notice what it can’t tell you. It says <code class="language-plaintext highlighter-rouge">Read</code> was called four times — not <em>which files</em>. It says <code class="language-plaintext highlighter-rouge">add_issue_comment</code> was called twice — not <em>what the comments said</em>, or whether the first attempt failed and the second was a retry. It gives one cumulative token number — not which turn was expensive. It records the final summary string — not the reasoning that produced it. <code class="language-plaintext highlighter-rouge">toolStats</code> is an aggregate; the trace file is the transcript.</p> <table> <thead> <tr> <th> </th> <th>Parent <code class="language-plaintext highlighter-rouge">toolUseResult</code> (rollup)</th> <th>Subagent trace file (evidence)</th> </tr> </thead> <tbody> <tr> <td>Granularity</td> <td>One line covering the whole run</td> <td>Every model turn, every tool call, every result</td> </tr> <tr> <td>Tokens</td> <td>One cumulative <code class="language-plaintext highlighter-rouge">totalTokens</code> + a <code class="language-plaintext highlighter-rouge">usage</code> sum</td> <td>Per-<code class="language-plaintext highlighter-rouge">assistant</code>-line <code class="language-plaintext highlighter-rouge">message.usage</code></td> </tr> <tr> <td>Tools</td> <td>Per-tool <strong>counts</strong> (<code class="language-plaintext highlighter-rouge">{"Read": 4}</code>)</td> <td>Every <code class="language-plaintext highlighter-rouge">tool_use</code>/<code class="language-plaintext highlighter-rouge">tool_result</code> pair, full <code class="language-plaintext highlighter-rouge">input</code> and <code class="language-plaintext highlighter-rouge">content</code></td> </tr> <tr> <td>Duration</td> <td>One <code class="language-plaintext highlighter-rouge">totalDurationMs</code> scalar</td> <td>Per-line <code class="language-plaintext highlighter-rouge">timestamp</code>s — diff them yourself</td> </tr> <tr> <td>Reasoning</td> <td>Absent</td> <td><code class="language-plaintext highlighter-rouge">thinking</code> blocks (when extended thinking is on)</td> </tr> </tbody> </table> <p>This is exactly why most parsers stop at the parent envelope and treat a subagent as a single opaque tool call. The data is <em>there</em> — it just lives in a different file, and you have to know to go get it. Anything that measures what a subagent actually did, rather than what the parent claims it did, has to read the trace.</p> <h2 id="what-you-wont-find-in-a-trace-file">What you won’t find in a trace file</h2> <p>Subagent traces are more homogeneous than parent sessions. Across the Claude Code subagent files sampled for the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md">reference doc</a> (v2.1.150), only three <code class="language-plaintext highlighter-rouge">type</code> values appear: <code class="language-plaintext highlighter-rouge">assistant</code>, <code class="language-plaintext highlighter-rouge">user</code>, and the occasional <code class="language-plaintext highlighter-rouge">attachment</code>.</p> <p>Conspicuously absent: <code class="language-plaintext highlighter-rouge">file-history-snapshot</code>, <code class="language-plaintext highlighter-rouge">system</code>, <code class="language-plaintext highlighter-rouge">permission-mode</code>, <code class="language-plaintext highlighter-rouge">ai-title</code>, <code class="language-plaintext highlighter-rouge">last-prompt</code>. Those are session-level concerns, and they stay in the parent file. The most consequential of these is <code class="language-plaintext highlighter-rouge">file-history-snapshot</code> — the backbone of <code class="language-plaintext highlighter-rouge">/rewind</code> that Part 2 covered. If a subagent edits code, the <em>edit</em> happens in the subagent’s run, but the <strong>snapshot line is written to the parent session</strong>, not the trace file. So a tool reconstructing edit history from snapshots reads the parent and will see subagent edits there; a tool reconstructing <em>which agent made an edit</em> has to correlate the snapshot’s timing against the trace. The rewind machinery is centralized in the parent; it doesn’t fork per subagent.</p> <p>The practical upshot: a Claude Code trace file is mostly long runs of alternating <code class="language-plaintext highlighter-rouge">assistant</code>/<code class="language-plaintext highlighter-rouge">user</code> lines. Anything that parses parent sessions can parse a trace with the same code path, given five adjustments — <code class="language-plaintext highlighter-rouge">isSidechain: true</code> on every line, <code class="language-plaintext highlighter-rouge">agentId</code> on every line, <code class="language-plaintext highlighter-rouge">attributionAgent</code> on every <code class="language-plaintext highlighter-rouge">assistant</code> line, <code class="language-plaintext highlighter-rouge">promptId</code> on every <code class="language-plaintext highlighter-rouge">user</code> line, and <code class="language-plaintext highlighter-rouge">sourceToolAssistantUUID</code> on tool-result lines only.</p> <h2 id="the-one-gap-worth-flagging-nesting">The one gap worth flagging: nesting</h2> <p>Everything above is verified against <strong>Claude Code v2.1.150</strong>, and one of its properties is load-bearing for the simple layout: <strong>Claude Code restricts subagents from invoking further subagents.</strong> Because of that restriction, every subagent in a Claude Code session originates from the parent, and every trace file sits directly under <code class="language-plaintext highlighter-rouge">&lt;session-uuid&gt;/subagents/</code>. The flat layout isn’t a design choice you can rely on universally — it’s a consequence of the runtime forbidding nested invocation. No nested calls, so no nesting.</p> <p>The <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md#nesting">Agent SDK</a> writes session files in the same JSONL format but may <em>not</em> carry that restriction — subagents there may be able to invoke their own subagents. If they can, the on-disk shape of a multi-level call tree is <strong>not yet verified</strong>: it could be a flat <code class="language-plaintext highlighter-rouge">subagents/</code> directory holding the whole tree, a nested <code class="language-plaintext highlighter-rouge">subagents/&lt;agentId&gt;/subagents/...</code> layout, or something else.</p> <p>I’m naming this as a real gap, not hedging the whole post: for Claude Code, the flat single-level layout is solid. But if you’re writing a parser meant to handle both runtimes, don’t hard-code a fixed directory depth. Walk the directory structure, and reconstruct the call tree from <code class="language-plaintext highlighter-rouge">agentId</code> relationships in the data rather than from the shape of the path. That posture costs you nothing on flat Claude Code sessions and saves you from a fragile assumption the day a nested SDK trace shows up.</p> <h2 id="one-gotcha-before-part-4">One gotcha before Part 4</h2> <p>There’s a trap baked into the two-file split, and it deserves a flag here even though its full treatment is the next post’s job.</p> <p>A subagent’s token usage is reported in <strong>two places for the same work</strong>: on every <code class="language-plaintext highlighter-rouge">assistant</code> line <em>inside</em> the trace file (<code class="language-plaintext highlighter-rouge">message.usage</code>, per turn), and in the parent’s <code class="language-plaintext highlighter-rouge">toolUseResult.usage</code> / <code class="language-plaintext highlighter-rouge">totalTokens</code> (rolled up across the whole run). These are the <strong>same tokens, counted twice</strong>. Sum both sources and your session-cost number is inflated — and inflated in a way that doesn’t look obviously wrong, which is what makes it dangerous.</p> <p>The “right” way depends on which question you’re asking: do you need a quick total from the parent alone, the full breakdown that reads trace files but excludes the parent rollup, or just the subagent’s cost on its own? And, what about the four token kinds, <code class="language-plaintext highlighter-rouge">service_tier</code>, and per-model pricing? That’s a whole post. <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/.claude/specs/series-outline.md">Part 4 — “Token accounting is harder than it looks”</a> is where the double-count hazard gets the full treatment. For now, know that the same tokens live in both files, and don’t add them together.</p> <h2 id="try-it-on-your-own-sessions">Try it on your own sessions</h2> <p>Find a session that delegated to a subagent (the Part 2 rollup snippet will surface the <code class="language-plaintext highlighter-rouge">agentId</code>), then point these at the trace file. Both use defensive <code class="language-plaintext highlighter-rouge">?</code> operators so non-matching lines are skipped rather than raising errors — the same discipline from Part 2. They assume <code class="language-plaintext highlighter-rouge">jq</code> on bash or zsh (macOS, Linux, or Windows via WSL or Git Bash); native PowerShell users can install <code class="language-plaintext highlighter-rouge">jq</code> with <code class="language-plaintext highlighter-rouge">winget install jqlang.jq</code> and adapt the piping.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Count the subagent's model turns (every assistant line in the trace):</span>
<span class="nb">cat</span> ~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;/subagents/agent-&lt;agentId&gt;.jsonl <span class="se">\</span>
  | jq <span class="nt">-r</span> <span class="s1">'select(.isSidechain? == true and .type? == "assistant") | .uuid'</span> <span class="se">\</span>
  | <span class="nb">wc</span> <span class="nt">-l</span>

<span class="c"># List the tools the subagent ACTUALLY called, in order —</span>
<span class="c"># the evidence behind the parent's toolStats aggregate:</span>
<span class="nb">cat</span> ~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;/subagents/agent-&lt;agentId&gt;.jsonl <span class="se">\</span>
  | jq <span class="nt">-r</span> <span class="s1">'select(.type? == "assistant")
      | .message.content[]? | select(.type? == "tool_use") | .name'</span>
</code></pre></div></div> <p>The first snippet answers “how many turns did this subagent take?” — a number the parent rollup never gives you. The second one expands the parent’s <code class="language-plaintext highlighter-rouge">toolStats</code> counts back into an ordered list of what the subagent did, which is the difference between “called <code class="language-plaintext highlighter-rouge">Read</code> four times” and “read these four files, in this order, before commenting.” That expansion — from rollup back to transcript — is one of many reasons trace files are worth opening.</p> <h2 id="whats-next">What’s next</h2> <p>We’ve now seen both halves of a delegation: the parent’s rollup (Part 2) and the subagent’s full trace (this post). The recurring theme is that the same activity is recorded twice, at two granularities, in two files — and the token gotcha above is the sharpest edge of that fact.</p> <p><a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/.claude/specs/series-outline.md">Part 4</a> takes the gotcha seriously. “What did this session actually cost?” sounds like a sum, but between the four token kinds, cache reads priced differently from cache creation, <code class="language-plaintext highlighter-rouge">service_tier</code>, per-model pricing, and the double-count we just flagged, it’s the question this whole series has been circling. Token accounting is harder than it looks — and the data to get it right is all sitting in these files.</p> <p>The sources behind this post:</p> <ul> <li><strong>Reference grounding:</strong> <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md"><code class="language-plaintext highlighter-rouge">reference/subagent-traces.md</code></a></li> <li><strong>Series planning:</strong> <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/.claude/specs/series-outline.md"><code class="language-plaintext highlighter-rouge">series-outline.md</code></a></li> <li><strong>Synthetic fixtures</strong> — valid JSONL, so the <code class="language-plaintext highlighter-rouge">jq</code> snippets run against them without a real session: the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-agent-invocation.jsonl">parent invocation</a> and the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-subagent-trace.jsonl">subagent trace</a></li> </ul> <p>If your own traces show a <code class="language-plaintext highlighter-rouge">type</code> value, an attribution field, or a nesting layout I haven’t described — especially from the Agent SDK — that’s exactly the kind of thing that updates the reference docs and sharpens later posts. Please let me know by creating an issue in the <code class="language-plaintext highlighter-rouge">claude-code-sessions</code> repo linked above.</p> <hr/> <p><em>Drafted with Claude Code (verified against v2.1.150). The ideas, claims, and any errors are mine.</em></p>]]></content><author><name></name></author><category term="foundation"/><category term="claude-code"/><category term="agent-sdk"/><category term="jsonl"/><category term="sessions"/><category term="subagents"/><summary type="html"><![CDATA[Part 3 of the anatomy series. When Claude Code delegates to a subagent, the parent session records only a rollup — the subagent's real work lives in a separate trace file. Where it is, how it links back, and why isSidechain is the field that matters.]]></summary></entry><entry><title type="html">How often does Claude retry a tool call?</title><link href="https://frederick-douglas-pearce.github.io/blog/2026/how-often-does-claude-retry-a-tool-call/" rel="alternate" type="text/html" title="How often does Claude retry a tool call?"/><published>2026-06-08T08:00:00+00:00</published><updated>2026-06-08T08:00:00+00:00</updated><id>https://frederick-douglas-pearce.github.io/blog/2026/how-often-does-claude-retry-a-tool-call</id><content type="html" xml:base="https://frederick-douglas-pearce.github.io/blog/2026/how-often-does-claude-retry-a-tool-call/"><![CDATA[<p><a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/posts/2026-06-04-reading-a-claude-code-session-line-by-line.md">Part 2 of this series</a> introduced <code class="language-plaintext highlighter-rouge">tool_use_id</code> as the pairing key that ties every tool call to its result. Every <code class="language-plaintext highlighter-rouge">tool_use</code> block on an <code class="language-plaintext highlighter-rouge">assistant</code> line carries an <code class="language-plaintext highlighter-rouge">id</code>; the corresponding <code class="language-plaintext highlighter-rouge">tool_result</code> block on the next <code class="language-plaintext highlighter-rouge">user</code> line carries a <code class="language-plaintext highlighter-rouge">tool_use_id</code> that matches it. Walk those pairs and you’ve reconstructed the tool history of an entire session.</p> <p>The natural next question is: what does that pairing key let you <em>measure</em>?</p> <p>Tool-call retry rate is one of the simplest signals to compute from the JSONL — three fields and a join. The answer is more telling than the simplicity suggests: different tools retry at very different rates, in patterns that say something about both the work being done and each tool’s failure modes.</p> <p>This is a standalone analysis post, not part of the foundation series. It’s meant to be short: one signal, one heuristic, one worked example.</p> <hr/> <h2 id="defining-retry-from-the-jsonl">Defining “retry” from the JSONL</h2> <p>A retry, for this purpose, is an <code class="language-plaintext highlighter-rouge">assistant</code> line that invokes a tool by name when a prior invocation of the same tool — in the same session — returned with <code class="language-plaintext highlighter-rouge">is_error: true</code> on its <code class="language-plaintext highlighter-rouge">tool_result</code> block.</p> <p>The exact fields involved:</p> <ul> <li><code class="language-plaintext highlighter-rouge">message.content[].type == "tool_use"</code> and <code class="language-plaintext highlighter-rouge">.name</code> — the tool being invoked and its name.</li> <li><code class="language-plaintext highlighter-rouge">message.content[].type == "tool_result"</code> and <code class="language-plaintext highlighter-rouge">.is_error == true</code> — the result block, nested inside a <code class="language-plaintext highlighter-rouge">user</code> line, flagging the failure.</li> </ul> <p><a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/tool-invocation.md#error-patterns"><code class="language-plaintext highlighter-rouge">reference/tool-invocation.md</code></a> documents both fields. The short version: <code class="language-plaintext highlighter-rouge">is_error</code> is an optional boolean on <code class="language-plaintext highlighter-rouge">tool_result</code>; it’s absent on the happy path and <code class="language-plaintext highlighter-rouge">true</code> when the tool reported a failure. The <code class="language-plaintext highlighter-rouge">tool_use_id</code> key links each <code class="language-plaintext highlighter-rouge">tool_result</code> back to its originating <code class="language-plaintext highlighter-rouge">tool_use</code> — that’s how you know which tool name corresponds to the error.</p> <p>Walking the cycle in order:</p> <ol> <li>Index each <code class="language-plaintext highlighter-rouge">tool_use</code> block by its <code class="language-plaintext highlighter-rouge">id</code> as you see it. Note the tool <code class="language-plaintext highlighter-rouge">name</code>.</li> <li>When you encounter a <code class="language-plaintext highlighter-rouge">tool_result</code> with <code class="language-plaintext highlighter-rouge">is_error: true</code>, look up the tool name via <code class="language-plaintext highlighter-rouge">tool_use_id</code>.</li> <li>If a later <code class="language-plaintext highlighter-rouge">assistant</code> line invokes a <code class="language-plaintext highlighter-rouge">tool_use</code> with the same <code class="language-plaintext highlighter-rouge">name</code>, that’s a retry.</li> </ol> <p>A few edge cases worth naming before moving on. This heuristic — a rule of thumb, not a guarantee — is <strong>error-driven</strong>: it only counts retries triggered by explicit <code class="language-plaintext highlighter-rouge">is_error: true</code> flags. The model may also retry in response to partial or confusing output without <code class="language-plaintext highlighter-rouge">is_error</code> being set. A <code class="language-plaintext highlighter-rouge">Bash</code> command that exits zero but produces unexpected output, for example, might prompt a second <code class="language-plaintext highlighter-rouge">Bash</code> call that looks identical to a retry but carries no error signal in the JSONL. Separately, a user resubmitting a prompt mid-session can trigger a tool re-invocation that the heuristic would count as a retry when it isn’t one in any meaningful sense.</p> <p>The alternative is to compare consecutive same-tool calls by input similarity, regardless of the error signal — that catches the quiet retries this heuristic misses, at the cost of per-tool tuning to define “similar enough” across very different input schemas. For a first signal, error-driven wins on precision: every result it surfaces is a case where Claude explicitly received an error and tried again.</p> <hr/> <h2 id="the-jq-to-extract-it">The <code class="language-plaintext highlighter-rouge">jq</code> to extract it</h2> <p>Two snippets. The first builds the per-session tool-call list with error flags. The second counts retries per tool name.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Per-session tool invocations with their error status.</span>
<span class="c"># Produces one line per tool call: {"name": "Bash", "id": "toolu_...", "is_error": false}</span>
jq <span class="nt">-r</span> <span class="s1">'
  if .type == "assistant" then
    .message.content[]? | select(.type == "tool_use") |
    {name: .name, id: .id, is_error: false}
  elif .type == "user" then
    .message.content[]? | select(.type == "tool_result") |
    {tool_use_id: .tool_use_id, is_error: (.is_error // false)}
  else empty end
'</span> &lt;session&gt;.jsonl
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Retry count per tool name: tool calls that followed an is_error: true result.</span>
<span class="c"># Works by joining on tool_use_id, then counting same-name calls after any error.</span>
jq <span class="nt">-rn</span> <span class="s1">'
  [inputs] |
  # Build a map of tool_use_id -&gt; name from assistant lines
  (map(select(.type == "assistant") |
    .message.content[]? | select(.type == "tool_use") |
    {(.id): .name}) | add) as $names |
  # Find all tool_use_ids that returned is_error: true
  ([.[] | select(.type == "user") |
    .message.content[]? |
    select(.type == "tool_result" and .is_error == true) |
    $names[.tool_use_id]] | group_by(.) |
    map({(.[0]): length}) | add // {}) as $errors |
  $errors
'</span> &lt;session&gt;.jsonl
</code></pre></div></div> <p>Run the second snippet with <code class="language-plaintext highlighter-rouge">jq -rn --slurp</code> or pipe it with <code class="language-plaintext highlighter-rouge">--null-input</code> depending on your <code class="language-plaintext highlighter-rouge">jq</code> version. The <code class="language-plaintext highlighter-rouge">(... // false)</code> on <code class="language-plaintext highlighter-rouge">is_error</code> handles the common case where the field is absent on successful results — absence and <code class="language-plaintext highlighter-rouge">false</code> mean the same thing here.</p> <p>The <code class="language-plaintext highlighter-rouge">$names</code> map connects each error result back to the tool that caused it — without it, you’d know an error occurred but not which tool was responsible.</p> <hr/> <h2 id="worked-example-one-retry-walked-end-to-end">Worked example: one retry, walked end to end</h2> <p>Here’s a single retry from a real session — an <code class="language-plaintext highlighter-rouge">Explore</code> subagent dispatched by the <code class="language-plaintext highlighter-rouge">/simplify</code> skill, paging through a saved diff file. The cycle comes from a <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md">subagent trace</a> rather than a parent session, but the pairing pattern is identical: same <code class="language-plaintext highlighter-rouge">tool_use</code> → <code class="language-plaintext highlighter-rouge">tool_result</code> blocks, same <code class="language-plaintext highlighter-rouge">tool_use_id</code> linkage.</p> <p>The first call passes <code class="language-plaintext highlighter-rouge">Read</code>’s <code class="language-plaintext highlighter-rouge">offset</code> as an array:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_use"</span><span class="p">,</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"toolu_…7pg"</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Read"</span><span class="p">,</span><span class="w"> </span><span class="nl">"input"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"file_path"</span><span class="p">:</span><span class="w"> </span><span class="s2">"/tmp/simplify_481.diff"</span><span class="p">,</span><span class="w"> </span><span class="nl">"offset"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="mi">447</span><span class="p">,</span><span class="w"> </span><span class="mi">525</span><span class="p">]</span><span class="w"> </span><span class="p">}</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">offset</code> is supposed to be a single starting line number. The result comes back flagged:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_result"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"tool_use_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"toolu_…7pg"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"is_error"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="s2">"&lt;tool_use_error&gt;InputValidationError: Read failed due to the following issue:</span><span class="se">\n</span><span class="s2">The parameter `offset` type is expected as `number` but provided as `array`&lt;/tool_use_error&gt;"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">tool_use_id</code> on the result matches the <code class="language-plaintext highlighter-rouge">id</code> on the call — the pairing key earning its keep. On the next assistant turn, the model fixes the shape:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_use"</span><span class="p">,</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"toolu_…As"</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Read"</span><span class="p">,</span><span class="w"> </span><span class="nl">"input"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"file_path"</span><span class="p">:</span><span class="w"> </span><span class="s2">"/tmp/simplify_481.diff"</span><span class="p">,</span><span class="w"> </span><span class="nl">"offset"</span><span class="p">:</span><span class="w"> </span><span class="mi">447</span><span class="p">,</span><span class="w"> </span><span class="nl">"limit"</span><span class="p">:</span><span class="w"> </span><span class="mi">80</span><span class="w"> </span><span class="p">}</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>Same tool, corrected input, succeeds. By the heuristic’s definition — a same-name <code class="language-plaintext highlighter-rouge">tool_use</code> following an <code class="language-plaintext highlighter-rouge">is_error: true</code> result for that tool — this is one unambiguous retry. The full trace lives at <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/sanitized/retry-rate-parameter-shape.jsonl"><code class="language-plaintext highlighter-rouge">fixtures/sanitized/retry-rate-parameter-shape.jsonl</code></a>; the three blocks above are lines 45, 46, and 47.<sup id="fnref:version"><a href="#fn:version" class="footnote" rel="footnote" role="doc-noteref">1</a></sup></p> <hr/> <h2 id="aggregating-across-one-session">Aggregating across one session</h2> <p>A single cycle shows the heuristic working. A session-wide distribution shows what it surfaces when you aggregate. From a different real session — sanitizer-PRD work on this repo, with intermixed <code class="language-plaintext highlighter-rouge">Bash</code>, <code class="language-plaintext highlighter-rouge">Edit</code>, <code class="language-plaintext highlighter-rouge">Read</code>, and <code class="language-plaintext highlighter-rouge">Write</code> activity, fixture at <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/sanitized/retry-rate-mixed-tools.jsonl"><code class="language-plaintext highlighter-rouge">retry-rate-mixed-tools.jsonl</code></a> — 3 of 56 tool calls returned <code class="language-plaintext highlighter-rouge">is_error: true</code>:</p> <table> <thead> <tr> <th>Tool</th> <th style="text-align: right">Errors / Calls</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Bash</code></td> <td style="text-align: right">1 / 20</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Edit</code></td> <td style="text-align: right">2 / 19</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Read</code></td> <td style="text-align: right">0 / 13</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Write</code></td> <td style="text-align: right">0 / 2</td> </tr> </tbody> </table> <p>The non-zero entries cluster on the tools that operate against mutable state — a <code class="language-plaintext highlighter-rouge">Bash</code> command that fails because a dependency isn’t installed yet, an <code class="language-plaintext highlighter-rouge">Edit</code> whose target string already changed. <code class="language-plaintext highlighter-rouge">Read</code> and <code class="language-plaintext highlighter-rouge">Write</code> can fail in principle but do so rarely. The relative order of <code class="language-plaintext highlighter-rouge">Bash</code> and <code class="language-plaintext highlighter-rouge">Edit</code> depends entirely on the work mix; what holds across sessions is the clustering, not the ranking.</p> <hr/> <h2 id="what-the-numbers-mean--and-dont-mean">What the numbers mean — and don’t mean</h2> <p><strong>The error-driven definition wins on precision.</strong> Across a 46-session corpus, a looser approach — counting any repeat call to the same tool as a “retry,” regardless of the error flag — fired 155 times. 119 of those (77%) had no error at all; they were ordinary sequential work mistaken for recovery. The cleanest false positive — a case where the heuristic fires but there’s no actual retry — was one run that flagged an agent for “retrying” <code class="language-plaintext highlighter-rouge">mcp__github__get_issue</code> six times. It wasn’t retrying anything; it was reading GitHub issues 465, 466, 467, 468, 469 in order. Five different inputs, zero errors, counted as five retries. Gating on <code class="language-plaintext highlighter-rouge">is_error: true</code> makes that enumeration vanish from the count, while leaving the genuine array-vs-number recovery above in.</p> <p><strong>A non-zero <code class="language-plaintext highlighter-rouge">Bash</code> retry rate is expected.</strong> <code class="language-plaintext highlighter-rouge">Bash</code> is where Claude runs builds, tests, installs, and environment checks. These commands fail for reasons that have nothing to do with Claude’s reasoning — a dependency not yet installed, a port in use, an output format that changes with tool version. A non-zero <code class="language-plaintext highlighter-rouge">Bash</code> retry rate isn’t a sign that something went wrong; it’s a sign that the session involved real operations against a real environment. The model is supposed to try again after these errors. How high “normal” is depends entirely on the work mix — a session doing environment setup will sit much higher than the 1-of-20 (5.0%) in the aggregate table above, which was a content-editing session.</p> <p><strong>A high <code class="language-plaintext highlighter-rouge">Edit</code> retry rate is more diagnostic.</strong> <code class="language-plaintext highlighter-rouge">Edit</code> uses string matching (<code class="language-plaintext highlighter-rouge">old_string</code> must appear exactly in the file). Retries here usually mean the file changed between when Claude read it and when it tried to edit it — either because Claude’s own earlier <code class="language-plaintext highlighter-rouge">Bash</code> commands modified it, or because the model read stale context. An <code class="language-plaintext highlighter-rouge">Edit</code> retry rate above ~20% across many sessions is worth investigating: it’s a signal that the session has a context-freshness problem, not just an environment-variability problem.</p> <p><strong>Near-zero <code class="language-plaintext highlighter-rouge">Read</code> rate is normal — but when <code class="language-plaintext highlighter-rouge">Read</code> does fail, it’s usually a parameter-type mismatch.</strong> <code class="language-plaintext highlighter-rouge">Read</code> errors are uncommon (the aggregate session above had zero), but the worked-example retry is the recurring kind when they do happen: an <code class="language-plaintext highlighter-rouge">offset</code> or <code class="language-plaintext highlighter-rouge">limit</code> parameter passed as the wrong type (an array instead of a number, for instance). A <code class="language-plaintext highlighter-rouge">Read</code> retry rate above a few percent in a dataset is worth inspecting individually — either the model is exploring paths it hasn’t confirmed exist, or it’s mis-shaping parameters.</p> <p><strong>Rate near 1.0 on any tool is an interruption signal, not a style signal.</strong> If you aggregate across many sessions and find one tool with a near-100% retry rate, the likely explanation isn’t that Claude is especially bad with that tool — it’s that the sessions in that slice were short or interrupted, leaving a small total where any single retry dominates the rate.</p> <p><strong>n=1 is below threshold for any strong claim.</strong> The example above is one session, one task type, one environment. The per-tool expectations above are directional based on how each tool operates — not statistical findings from a broad sample. AgentFluent’s value is running this analysis across many sessions and surfacing per-tool distributions wide enough to make the rates meaningful.</p> <p>One more thing the heuristic doesn’t distinguish: an immediate retry (Claude sees the error and tries again in the next turn) versus a deferred re-invocation (Claude does other work, then comes back to the same tool much later). Both look identical under this definition. The immediate retry is more clearly a recovery response; the deferred one might be coincidental reuse. If you care about the distinction, you’d need to add a limit on how many turns apart the two calls can be.</p> <hr/> <h2 id="why-the-rate-is-worth-tracking">Why the rate is worth tracking</h2> <p>Every retry has a cost. The failed call, its error payload, and the corrected call all consume tokens and occupy the context window before any useful result lands — and they add latency to the turn. On a single session that’s noise; aggregated across many runs, a tool that retries often is a recurring tax on both spend and wall-clock time.</p> <p>The more useful point is that the rate is <em>actionable</em>, and often the fix is on the tool side, not the model side. A tool that retries a lot frequently has a definition that doesn’t constrain the call well enough. The <code class="language-plaintext highlighter-rouge">Read</code> case above is canonical: <code class="language-plaintext highlighter-rouge">offset</code> expects a number, the model passed an array, and nothing in the tool’s schema or description steered it away. Anthropic’s own guidance on <a href="https://www.anthropic.com/engineering/writing-tools-for-agents">writing effective tools for agents</a> recommends exactly the remedies that move this rate — unambiguously named parameters, descriptions that make implicit context explicit, and examples of correctly formatted inputs surfaced in error responses. A per-tool retry rate, tracked across many sessions, is how you find which tools earn that investment first.</p> <hr/> <h2 id="whats-next">What’s next</h2> <p>This kind of analysis — defined heuristically, run across many sessions, surfaced as per-tool diagnostics — is what <a href="https://github.com/frederick-douglas-pearce/agentfluent">AgentFluent</a> automates at scale. Its <code class="language-plaintext highlighter-rouge">parameter_retry</code> signal extracts the <em>corrected</em> call alongside the error — so instead of just knowing that <code class="language-plaintext highlighter-rouge">Read</code> failed, you see the exact input shape that succeeded (<code class="language-plaintext highlighter-rouge">{"file_path": "…", "offset": 447, "limit": 80}</code>).</p> <p>The session JSONL has more signals like this one: tool call latency distributions (from <code class="language-plaintext highlighter-rouge">toolUseResult.durationMs</code>), edit-to-error ratios, the ratio of cache reads to fresh input tokens as a measure of context efficiency. Each is derivable from fields already introduced in Parts 1 and 2. This post is the first in an occasional analysis cadence — concrete measurements from the format, grounded in specific fields, with honest limits on what a single session’s numbers can support.</p> <hr/> <p><em>Drafted with Claude Code (verified against v2.1.150). The ideas, claims, and any errors are mine.</em></p> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:version"> <p>The single-retry cycle above was captured against Claude Code v2.1.158; the rest of this post’s structural claims (field names, pairing key, error flag) are verified against v2.1.150 as noted in the front matter. The fields in question — <code class="language-plaintext highlighter-rouge">tool_use.id</code>, <code class="language-plaintext highlighter-rouge">tool_result.tool_use_id</code>, <code class="language-plaintext highlighter-rouge">is_error</code> — have been stable across v2.1.150 and v2.1.158. <a href="#fnref:version" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="analysis"/><category term="claude-code"/><category term="jsonl"/><category term="sessions"/><category term="tool-use"/><summary type="html"><![CDATA[The session JSONL's tool_use_id pairing key (introduced in Part 2) lets you measure how often Claude retries after a tool error — and the answer is different by tool, which is where it gets interesting.]]></summary></entry><entry><title type="html">Reading a Claude Code session, line by line</title><link href="https://frederick-douglas-pearce.github.io/blog/2026/reading-a-claude-code-session-line-by-line/" rel="alternate" type="text/html" title="Reading a Claude Code session, line by line"/><published>2026-06-04T08:00:00+00:00</published><updated>2026-06-04T08:00:00+00:00</updated><id>https://frederick-douglas-pearce.github.io/blog/2026/reading-a-claude-code-session-line-by-line</id><content type="html" xml:base="https://frederick-douglas-pearce.github.io/blog/2026/reading-a-claude-code-session-line-by-line/"><![CDATA[<p>Open any Claude Code session JSONL file and one structural quirk jumps out within a few lines: some field names use <code class="language-plaintext highlighter-rouge">snake_case</code> and others use <code class="language-plaintext highlighter-rouge">camelCase</code>. <code class="language-plaintext highlighter-rouge">tool_use_id</code> and <code class="language-plaintext highlighter-rouge">stop_reason</code> next to <code class="language-plaintext highlighter-rouge">parentUuid</code> and <code class="language-plaintext highlighter-rouge">toolUseResult</code>. It looks inconsistent.</p> <p>It isn’t — it’s a tell. <code class="language-plaintext highlighter-rouge">snake_case</code> fields are generally content the Anthropic API defines: things the model produces and consumes. <code class="language-plaintext highlighter-rouge">camelCase</code> fields are envelope metadata that Claude Code’s harness adds on top — session identifiers, parent UUIDs, tool-invocation rollups. Once that split clicks, the format stops looking messy and starts providing architectural insight: Anthropic API conversations are always wrapped in Claude Code’s bookkeeping.</p> <p>This is Part 2 of the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/posts/2026-05-26-anatomy-of-a-claude-code-session.md">Anatomy of a Claude Code session</a> series. Part 1 covered where session files live and what Claude Code itself uses them for. This post is the structural tour: every type of line you’ll find inside a session, the two layers (API content and harness metadata) that compose them, why tool results live inside <code class="language-plaintext highlighter-rouge">user</code> messages, and what the <code class="language-plaintext highlighter-rouge">toolUseResult</code> envelope carries that the model never sees.</p> <p>By the end you’ll have a mental model for every distinct <code class="language-plaintext highlighter-rouge">type</code> value in a session JSONL — including the many “metadata” types that most parsers silently drop — and three <code class="language-plaintext highlighter-rouge">jq</code> snippets to start asking your own questions of any session file on your disk.</p> <p>If you want field-level definitions while reading, <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/data-dictionary.md"><code class="language-plaintext highlighter-rouge">reference/data-dictionary.md</code></a> in the repo is the source of truth. This post is the narrative; that doc is the lookup.</p> <hr/> <h2 id="the-taxonomy-at-a-glance">The taxonomy at a glance</h2> <p>Every line in a session JSONL has a top-level <code class="language-plaintext highlighter-rouge">type</code> field. In v2.1.150 you’ll see <strong>at least</strong> the distinct values below. The list is observational — Anthropic doesn’t publicly catalogue the full set, and sessions in the wild can carry types this list hasn’t sampled yet. Treat the table as a strong starting point, not a closed taxonomy.</p> <table> <thead> <tr> <th>What it is</th> <th><code class="language-plaintext highlighter-rouge">type</code> values</th> </tr> </thead> <tbody> <tr> <td><strong>Activity</strong> — what you and the model said and did</td> <td><code class="language-plaintext highlighter-rouge">user</code>, <code class="language-plaintext highlighter-rouge">assistant</code></td> </tr> <tr> <td><strong>State</strong> — what the session knows about itself</td> <td><code class="language-plaintext highlighter-rouge">file-history-snapshot</code>, <code class="language-plaintext highlighter-rouge">system</code>, <code class="language-plaintext highlighter-rouge">permission-mode</code>, <code class="language-plaintext highlighter-rouge">ai-title</code>, <code class="language-plaintext highlighter-rouge">last-prompt</code>, <code class="language-plaintext highlighter-rouge">attachment</code></td> </tr> <tr> <td><strong>Telemetry</strong> — events emitted as things happen</td> <td><code class="language-plaintext highlighter-rouge">progress</code>, <code class="language-plaintext highlighter-rouge">hook_progress</code>, <code class="language-plaintext highlighter-rouge">bash_progress</code>, <code class="language-plaintext highlighter-rouge">queue-operation</code></td> </tr> </tbody> </table> <p>Two types I’ve seen in real sessions but haven’t independently verified at the time of writing are <code class="language-plaintext highlighter-rouge">custom-title</code> (which appears to be the user-set session title, distinct from the auto-generated <code class="language-plaintext highlighter-rouge">ai-title</code>) and <code class="language-plaintext highlighter-rouge">pr-link</code> (the per-session PR association mentioned in Part 1). They’re not in the table above because the reference doc hasn’t catalogued them yet — but if you run the <code class="language-plaintext highlighter-rouge">jq</code> snippet at the end of this post against your own sessions, expect to see them and other types appear.</p> <p>The first bucket is what most parsers think a “session” is. The second is what <code class="language-plaintext highlighter-rouge">/rewind</code>, the resume picker, and the permission UI all read. The third is the chatter the harness emits while you’re working — usually irrelevant for after-the-fact analysis, sometimes invaluable for debugging.</p> <p>I’ll walk each bucket in turn, then come back to the structural twist that hides inside <code class="language-plaintext highlighter-rouge">user</code> messages.</p> <hr/> <h2 id="assistant--the-models-voice"><code class="language-plaintext highlighter-rouge">assistant</code> — the model’s voice</h2> <p>This is the line type with the richest internal structure, because everything the model produces gets packed into it. The top-level envelope (<code class="language-plaintext highlighter-rouge">type</code>, <code class="language-plaintext highlighter-rouge">sessionId</code>, <code class="language-plaintext highlighter-rouge">uuid</code>, <code class="language-plaintext highlighter-rouge">parentUuid</code>, <code class="language-plaintext highlighter-rouge">timestamp</code>, <code class="language-plaintext highlighter-rouge">cwd</code>, <code class="language-plaintext highlighter-rouge">version</code>) is the same as on every other line. The interesting payload lives inside <code class="language-plaintext highlighter-rouge">message</code>.</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"assistant"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"assistant"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"model"</span><span class="p">:</span><span class="w"> </span><span class="s2">"claude-sonnet-4-6"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"text"</span><span class="p">,</span><span class="w"> </span><span class="nl">"text"</span><span class="p">:</span><span class="w"> </span><span class="s2">"I'll read the file."</span><span class="w"> </span><span class="p">},</span><span class="w">
      </span><span class="p">{</span><span class="w">
        </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_use"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"toolu_synthetic_001"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Read"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"input"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"file_path"</span><span class="p">:</span><span class="w"> </span><span class="s2">"/home/dev/example-project/src/main.py"</span><span class="w"> </span><span class="p">}</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">],</span><span class="w">
    </span><span class="nl">"usage"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"input_tokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">18</span><span class="p">,</span><span class="w">
      </span><span class="nl">"output_tokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">35</span><span class="p">,</span><span class="w">
      </span><span class="nl">"cache_creation_input_tokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w">
      </span><span class="nl">"cache_read_input_tokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">1240</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="nl">"stop_reason"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_use"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>Three things to notice.</p> <p><strong><code class="language-plaintext highlighter-rouge">message.content</code> is an array of content blocks</strong>, not a string. A single assistant turn can carry multiple blocks of different types. The example above has two — a <code class="language-plaintext highlighter-rouge">text</code> block saying “I’ll read the file” followed by a <code class="language-plaintext highlighter-rouge">tool_use</code> block invoking Read. This is exactly what you’d expect when the model both speaks and reaches for a tool in the same turn. Parsers that assume “one text response per assistant line” will silently drop the tool call.</p> <p>The block types you’ll see inside <code class="language-plaintext highlighter-rouge">content</code>:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">text</code></strong> — plain text. Most common.</li> <li><strong><code class="language-plaintext highlighter-rouge">tool_use</code></strong> — the model invoking a tool. Carries an <code class="language-plaintext highlighter-rouge">id</code>, the tool <code class="language-plaintext highlighter-rouge">name</code>, and tool-specific <code class="language-plaintext highlighter-rouge">input</code>. Pairs with a later <code class="language-plaintext highlighter-rouge">tool_result</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">thinking</code></strong> — extended-thinking content blocks. Only present when extended thinking was enabled for the session. Includes a <code class="language-plaintext highlighter-rouge">signature</code> field; treat it as an opaque blob.</li> </ul> <p><strong><code class="language-plaintext highlighter-rouge">message.usage</code> is the only token accounting that matters.</strong> This is where input, output, cache-read, and cache-creation tokens are reported. There are no token counts on <code class="language-plaintext highlighter-rouge">user</code> lines — Claude Code doesn’t bill the user for typing. If you want session-level totals, you sum across <code class="language-plaintext highlighter-rouge">assistant</code> lines, and you need to account for the four token kinds separately because they’re priced differently. Part 1 went into this; the short version is “naive summing produces wrong dollar figures.”</p> <p><strong><code class="language-plaintext highlighter-rouge">stop_reason</code> tells you whether the model finished or paused.</strong> Two values dominate:</p> <ul> <li><code class="language-plaintext highlighter-rouge">"end_turn"</code> — the model is done. No tool call to come.</li> <li><code class="language-plaintext highlighter-rouge">"tool_use"</code> — the model stopped to invoke a tool. The very next user line should carry the matching <code class="language-plaintext highlighter-rouge">tool_result</code>.</li> </ul> <p>If you’re walking a session to reconstruct what happened, <code class="language-plaintext highlighter-rouge">stop_reason</code> is how you predict whether the next line will be a real user prompt or a tool response.</p> <hr/> <h2 id="user--and-the-structural-twist"><code class="language-plaintext highlighter-rouge">user</code> — and the structural twist</h2> <p><code class="language-plaintext highlighter-rouge">user</code> lines are simpler in their envelope but stranger in what they contain. The twist is this: <strong>tool results are not their own top-level type</strong>. They’re content blocks inside <code class="language-plaintext highlighter-rouge">user</code> messages.</p> <p>That is, when Claude Code finishes a <code class="language-plaintext highlighter-rouge">Read</code> and feeds the file contents back to the model, the file contents land in a <code class="language-plaintext highlighter-rouge">user</code> line. Not a <code class="language-plaintext highlighter-rouge">tool_result</code> line. Not a <code class="language-plaintext highlighter-rouge">system</code> line. A <code class="language-plaintext highlighter-rouge">user</code> line.</p> <p>This is the format’s most counterintuitive detail, and every parser author runs into it at least once. If you write a quick filter — “give me all the tool results in this session” — and filter by <code class="language-plaintext highlighter-rouge">type == "tool_result"</code> at the top level, you’ll get zero hits. Tool results are always nested inside <code class="language-plaintext highlighter-rouge">user.message.content</code> arrays.</p> <h3 id="two-shapes-of-messagecontent">Two shapes of <code class="language-plaintext highlighter-rouge">message.content</code></h3> <p>A <code class="language-plaintext highlighter-rouge">user</code> line carries one of two payload shapes:</p> <p><strong>Shape 1 — a string.</strong> This is a plain user prompt. The string is the literal text you typed.</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"user"</span><span class="p">,</span><span class="w"> </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"user"</span><span class="p">,</span><span class="w"> </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="s2">"What's in src/main.py?"</span><span class="w"> </span><span class="p">}</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>Shape 2 — an array of content blocks.</strong> This is how tool results come back, and occasionally how structured prompts (e.g., with attached context) are recorded.</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"user"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"user"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w">
        </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_result"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"tool_use_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"toolu_synthetic_001"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="s2">"def main():</span><span class="se">\n</span><span class="s2">    print('Hello, world!')</span><span class="se">\n</span><span class="s2">"</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">tool_use_id</code> on the result is the <strong>pairing key</strong> that ties this back to a prior <code class="language-plaintext highlighter-rouge">tool_use</code> block in some earlier <code class="language-plaintext highlighter-rouge">assistant</code> line. The IDs are unique within a session — they look like <code class="language-plaintext highlighter-rouge">toolu_&lt;base64-ish-suffix&gt;</code> in real sessions.</p> <h3 id="why-is-it-a-user-message">Why is it a user message?</h3> <p>Because in the Anthropic API, conversations alternate strictly between <code class="language-plaintext highlighter-rouge">user</code> and <code class="language-plaintext highlighter-rouge">assistant</code> roles. Tool results have to come back as one of those two, and the model can’t issue its own tool result — only the runtime can. So the runtime impersonates the user, packs the tool output into a <code class="language-plaintext highlighter-rouge">tool_result</code> content block, and emits it as a <code class="language-plaintext highlighter-rouge">user</code> line. The model reads that line as the next “user turn” and decides what to do next.</p> <p>This isn’t Claude Code being quirky. It’s <a href="https://platform.claude.com/docs/en/build-with-claude/tool-use">the Anthropic Messages API’s tool-use protocol</a> faithfully recorded. Once you see it that way, the alternation pattern becomes load-bearing for parsing: in any tool-using sequence, <code class="language-plaintext highlighter-rouge">assistant</code> (with <code class="language-plaintext highlighter-rouge">tool_use</code>) is always followed by <code class="language-plaintext highlighter-rouge">user</code> (with <code class="language-plaintext highlighter-rouge">tool_result</code>).</p> <hr/> <h2 id="the-tooluseresult-envelope">The <code class="language-plaintext highlighter-rouge">toolUseResult</code> envelope</h2> <p>Here’s the part that surprises readers who think they’ve figured the format out: when the underlying tool was a context-bearing one (<code class="language-plaintext highlighter-rouge">Agent</code>, <code class="language-plaintext highlighter-rouge">Bash</code>, <code class="language-plaintext highlighter-rouge">Edit</code>, <code class="language-plaintext highlighter-rouge">WebFetch</code>, and others), the <code class="language-plaintext highlighter-rouge">user</code> line that carries the tool result also carries a <strong>second top-level key</strong> called <code class="language-plaintext highlighter-rouge">toolUseResult</code>. It sits beside <code class="language-plaintext highlighter-rouge">message</code>, not inside it.</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"user"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"user"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w">
        </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_result"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"tool_use_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"toolu_synthetic_002"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Drafted acceptance criteria for issue #5..."</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"toolUseResult"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"status"</span><span class="p">:</span><span class="w"> </span><span class="s2">"success"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"agentId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"99999999-9999-9999-9999-999999999001"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"agentType"</span><span class="p">:</span><span class="w"> </span><span class="s2">"pm"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"totalDurationMs"</span><span class="p">:</span><span class="w"> </span><span class="mi">132140</span><span class="p">,</span><span class="w">
    </span><span class="nl">"totalTokens"</span><span class="p">:</span><span class="w"> </span><span class="mi">18234</span><span class="p">,</span><span class="w">
    </span><span class="nl">"totalToolUseCount"</span><span class="p">:</span><span class="w"> </span><span class="mi">7</span><span class="p">,</span><span class="w">
    </span><span class="nl">"toolStats"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"Read"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w"> </span><span class="nl">"mcp__github__get_issue"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="nl">"mcp__github__add_issue_comment"</span><span class="p">:</span><span class="w"> </span><span class="mi">2</span><span class="w"> </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>Two things stand out here. First, <strong>the field name uses camelCase</strong> (<code class="language-plaintext highlighter-rouge">toolUseResult</code>, <code class="language-plaintext highlighter-rouge">agentId</code>, <code class="language-plaintext highlighter-rouge">totalDurationMs</code>). Most of the message content inside <code class="language-plaintext highlighter-rouge">message</code> — the part that mirrors the Anthropic API’s content blocks — uses <code class="language-plaintext highlighter-rouge">snake_case</code>: <code class="language-plaintext highlighter-rouge">tool_use</code>, <code class="language-plaintext highlighter-rouge">tool_result</code>, <code class="language-plaintext highlighter-rouge">tool_use_id</code>, <code class="language-plaintext highlighter-rouge">input_tokens</code>, <code class="language-plaintext highlighter-rouge">stop_reason</code>. The outer Claude Code-specific envelope fields (including <code class="language-plaintext highlighter-rouge">sessionId</code>, <code class="language-plaintext highlighter-rouge">parentUuid</code>, <code class="language-plaintext highlighter-rouge">isSidechain</code>, and now <code class="language-plaintext highlighter-rouge">toolUseResult</code>) use camelCase. It’s a useful tell while you’re reading: camelCase generally means “harness-level metadata,” snake_case generally means “API-level content.”</p> <p>Second, <strong>the envelope lives outside <code class="language-plaintext highlighter-rouge">message.content</code></strong>, not inside it. That placement is meaningful: the contents of <code class="language-plaintext highlighter-rouge">message.content</code> are what gets sent back to the model. The envelope is metadata for the harness and any parser that comes later. The model never sees <code class="language-plaintext highlighter-rouge">toolUseResult</code>; you do.</p> <p>What’s in it depends on the tool. The full union of observed keys is in <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/data-dictionary.md#tooluseresult-envelope"><code class="language-plaintext highlighter-rouge">data-dictionary.md</code></a>; a few representative subsets:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">Agent</code></strong> — <code class="language-plaintext highlighter-rouge">agentId</code>, <code class="language-plaintext highlighter-rouge">agentType</code>, <code class="language-plaintext highlighter-rouge">prompt</code>, <code class="language-plaintext highlighter-rouge">totalDurationMs</code>, <code class="language-plaintext highlighter-rouge">totalTokens</code>, <code class="language-plaintext highlighter-rouge">totalToolUseCount</code>, <code class="language-plaintext highlighter-rouge">toolStats</code>. This is the surface that powers any per-subagent diagnostic.</li> <li><strong><code class="language-plaintext highlighter-rouge">Bash</code></strong> — <code class="language-plaintext highlighter-rouge">stdout</code>, <code class="language-plaintext highlighter-rouge">stderr</code>, <code class="language-plaintext highlighter-rouge">code</code>, <code class="language-plaintext highlighter-rouge">interrupted</code>, <code class="language-plaintext highlighter-rouge">durationMs</code>. Enough to characterize most command outcomes without parsing <code class="language-plaintext highlighter-rouge">stdout</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">Edit</code></strong> — <code class="language-plaintext highlighter-rouge">filePath</code>, <code class="language-plaintext highlighter-rouge">oldString</code>, <code class="language-plaintext highlighter-rouge">newString</code>, <code class="language-plaintext highlighter-rouge">replaceAll</code>, <code class="language-plaintext highlighter-rouge">structuredPatch</code>, <code class="language-plaintext highlighter-rouge">userModified</code>. The diff is in <code class="language-plaintext highlighter-rouge">structuredPatch</code>; security-review tooling lives off that field.</li> <li><strong><code class="language-plaintext highlighter-rouge">Read</code></strong> — often minimal or absent. The actionable content is in <code class="language-plaintext highlighter-rouge">tool_result.content</code>; <code class="language-plaintext highlighter-rouge">Read</code> doesn’t need a separate envelope.</li> </ul> <p>If you’re building anything that analyzes sessions — tool-usage patterns, subagent cost rollups, edit audits — <code class="language-plaintext highlighter-rouge">toolUseResult</code> is where most of the high-information signal lives. <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/tool-invocation.md"><code class="language-plaintext highlighter-rouge">reference/tool-invocation.md</code></a> breaks the envelope out per tool with the per-key semantics.</p> <hr/> <h2 id="the-skipped-types--and-why-some-of-them-arent">The “skipped” types — and why some of them aren’t</h2> <p>Beyond <code class="language-plaintext highlighter-rouge">user</code> and <code class="language-plaintext highlighter-rouge">assistant</code>, a session JSONL carries several other top-level types. Most parsers drop all of them. Some you can safely ignore. One in particular you can’t — not if you want to understand what <code class="language-plaintext highlighter-rouge">/rewind</code> is doing.</p> <h3 id="file-history-snapshot"><code class="language-plaintext highlighter-rouge">file-history-snapshot</code></h3> <p>This is the type that powers <code class="language-plaintext highlighter-rouge">/rewind</code>’s “restore code” capability. It’s not metadata. It’s the actual data structure that lets Claude Code put your files back the way they were.</p> <p>The top-level shape:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"file-history-snapshot"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"messageId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"&lt;some-message-uuid&gt;"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"isSnapshotUpdate"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"snapshot"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"messageId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
    </span><span class="nl">"timestamp"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
    </span><span class="nl">"trackedFileBackups"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"/abs/path/to/file.py"</span><span class="p">:</span><span class="w"> </span><span class="s2">"&lt;file contents before the edit&gt;"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"..."</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">trackedFileBackups</code> map is keyed by absolute file path; the values are the file’s contents <em>before</em> each Claude-tool edit. When you run <code class="language-plaintext highlighter-rouge">/rewind</code> and choose to restore code, Claude Code reads these entries in reverse chronological order and writes the prior contents back to disk.</p> <p>Part 1 inferred this connection from the docs (rewind persists across sessions and is cleaned up with sessions, therefore it has to live in the session files). Reading the JSONL confirms it directly: every Claude-tool edit produces a snapshot entry. If you want to audit which files Claude edited and when — or reproduce <code class="language-plaintext highlighter-rouge">/rewind</code>’s behavior in your own tools — these are the lines you read.</p> <p>This type also has a property worth noting: the snapshots cover only files Claude edited via its file-editing tools. Bash-driven changes (<code class="language-plaintext highlighter-rouge">rm</code>, <code class="language-plaintext highlighter-rouge">mv</code>, <code class="language-plaintext highlighter-rouge">cp</code>) and your own external edits aren’t here. The snapshots are scoped to the tool surface that emits them.</p> <h3 id="system"><code class="language-plaintext highlighter-rouge">system</code></h3> <p>Records internal Claude Code events: model switches mid-session, tool registry updates, harness-level state changes. Fields include <code class="language-plaintext highlighter-rouge">subtype</code>, <code class="language-plaintext highlighter-rouge">durationMs</code>, <code class="language-plaintext highlighter-rouge">isMeta</code>, and <code class="language-plaintext highlighter-rouge">content</code>.</p> <p>You’d usually skip these. The exception is diagnostic work — debugging why a session behaved oddly, or correlating a sudden model change with a behavior shift.</p> <h3 id="permission-mode"><code class="language-plaintext highlighter-rouge">permission-mode</code></h3> <p>A single-purpose line recording a permission-mode transition (e.g., from <code class="language-plaintext highlighter-rouge">"default"</code> to <code class="language-plaintext highlighter-rouge">"acceptEdits"</code> or <code class="language-plaintext highlighter-rouge">"bypassPermissions"</code>). Carries <code class="language-plaintext highlighter-rouge">permissionMode</code>, <code class="language-plaintext highlighter-rouge">sessionId</code>, and <code class="language-plaintext highlighter-rouge">type</code>.</p> <p>Useful for two things: auditing permission posture across a session, and correlating tool failures with the permission state at the time. Otherwise ignorable.</p> <h3 id="ai-title-last-prompt"><code class="language-plaintext highlighter-rouge">ai-title</code>, <code class="language-plaintext highlighter-rouge">last-prompt</code></h3> <p>Picker metadata. <code class="language-plaintext highlighter-rouge">ai-title</code> is the auto-generated session title you see when you run <code class="language-plaintext highlighter-rouge">/resume</code>. <code class="language-plaintext highlighter-rouge">last-prompt</code> is a pointer to the most recent user prompt — the picker uses it to render the preview line.</p> <p>Useful if you’re building a session index or a resume-picker UI outside Claude Code. Otherwise display chrome.</p> <h3 id="attachment"><code class="language-plaintext highlighter-rouge">attachment</code></h3> <p>A line representing an attachment (a pasted file, an image, a structured context blob) associated with a nearby message. Carries the standard envelope fields plus an <code class="language-plaintext highlighter-rouge">attachment</code> payload.</p> <p>Useful for recovering the full multimodal context of a turn. If you only read <code class="language-plaintext highlighter-rouge">user.message.content</code>, you’ll miss anything that was pasted in as an attachment rather than typed as inline text.</p> <h3 id="progress-hook_progress-bash_progress-queue-operation"><code class="language-plaintext highlighter-rouge">progress</code>, <code class="language-plaintext highlighter-rouge">hook_progress</code>, <code class="language-plaintext highlighter-rouge">bash_progress</code>, <code class="language-plaintext highlighter-rouge">queue-operation</code></h3> <p>Streaming events emitted as a tool runs, a hook executes, or the harness schedules work internally. Generally high-volume chatter. The data-dictionary notes that the first three weren’t observed in v2.1.150 sample sessions — they may still be emitted under specific conditions (long-running commands, hooks that emit progress), but presence is conditional.</p> <p>For real-time monitoring of a session-in-progress (<code class="language-plaintext highlighter-rouge">tail -f</code> on the JSONL), these are gold. For after-the-fact analysis, they’re usually noise.</p> <hr/> <h2 id="putting-it-together">Putting it together</h2> <p>If you sit down with a real session JSONL in <code class="language-plaintext highlighter-rouge">~/.claude/projects/&lt;slug&gt;/</code> and start parsing it now, here’s the mental model that holds up:</p> <ol> <li><strong>Walk top to bottom.</strong> The file is in chronological order. <code class="language-plaintext highlighter-rouge">parentUuid</code> chains messages together; <code class="language-plaintext highlighter-rouge">parentUuid: null</code> means a turn-starter.</li> <li><strong>Index <code class="language-plaintext highlighter-rouge">tool_use</code> blocks by <code class="language-plaintext highlighter-rouge">id</code> as you go.</strong> Every <code class="language-plaintext highlighter-rouge">tool_use.id</code> should later appear as a <code class="language-plaintext highlighter-rouge">tool_result.tool_use_id</code> in some downstream <code class="language-plaintext highlighter-rouge">user</code> line. Missing pairs almost always mean an interrupted session.</li> <li><strong>For each tool-result-carrying <code class="language-plaintext highlighter-rouge">user</code> line, also check for a sibling <code class="language-plaintext highlighter-rouge">toolUseResult</code>.</strong> That’s where the rich metadata lives — duration, tokens, tool stats, status. It’s at the top level, not inside <code class="language-plaintext highlighter-rouge">message.content</code>, and it uses camelCase.</li> <li><strong>Decide explicitly what to do with the “skipped” types.</strong> If you only care about user-visible activity, drop them all. If you care about file edits over time, keep <code class="language-plaintext highlighter-rouge">file-history-snapshot</code>. If you care about session orchestration or diagnostics, keep <code class="language-plaintext highlighter-rouge">system</code> and <code class="language-plaintext highlighter-rouge">permission-mode</code>.</li> </ol> <p>Three lines of <code class="language-plaintext highlighter-rouge">jq</code> that take this from theory to practice. Run them on any of your session files. Snippets below assume <code class="language-plaintext highlighter-rouge">jq</code> on bash or zsh (macOS, Linux, or Windows via WSL or Git Bash); native PowerShell users can install <code class="language-plaintext highlighter-rouge">jq</code> via <code class="language-plaintext highlighter-rouge">winget install jqlang.jq</code> and adapt the piping syntax.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Every distinct top-level type in the file, with counts:</span>
<span class="nb">cat</span> &lt;session&gt;.jsonl | jq <span class="nt">-r</span> <span class="s1">'.type'</span> | <span class="nb">sort</span> | <span class="nb">uniq</span> <span class="nt">-c</span>

<span class="c"># Every tool the assistant invoked, in order:</span>
<span class="nb">cat</span> &lt;session&gt;.jsonl | jq <span class="nt">-r</span> <span class="s1">'select(.type=="assistant")
  | .message.content[]? | select(.type=="tool_use") | .name'</span>

<span class="c"># Every subagent invocation with its rollup:</span>
<span class="nb">cat</span> &lt;session&gt;.jsonl | jq <span class="s1">'select(.toolUseResult?.agentType?)
  | {agent: .toolUseResult.agentType,
     agent_id: .toolUseResult.agentId,
     duration_ms: .toolUseResult.totalDurationMs,
     tokens: .toolUseResult.totalTokens}'</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">?</code> operators in the third snippet matter: in real sessions, <code class="language-plaintext highlighter-rouge">toolUseResult</code> is sometimes an array or a string rather than an object, and without the <code class="language-plaintext highlighter-rouge">?</code> guards <code class="language-plaintext highlighter-rouge">jq</code> will raise an indexing error on those lines. With them, non-matching lines are silently skipped.</p> <p>The first snippet is the fastest way to feel the shape of a session you’ve never opened before — and the easiest way to discover top-level <code class="language-plaintext highlighter-rouge">type</code> values this post hasn’t catalogued. The second is a one-line agent-trace. The third pulls the rollup of every subagent the parent session delegated to. The <code class="language-plaintext highlighter-rouge">agent_id</code> value in that rollup is the literal handle that takes you to the subagent’s full trace file at <code class="language-plaintext highlighter-rouge">~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;/subagents/agent-&lt;agent_id&gt;.jsonl</code> — which is where Part 3 picks up. I deliberately omitted <code class="language-plaintext highlighter-rouge">toolStats</code> from the third snippet because its shape varies by agent type — built-in subagents and the <code class="language-plaintext highlighter-rouge">Explore</code> agent populate different keys.</p> <hr/> <h2 id="whats-next">What’s next</h2> <p>This post stopped at the parent-session level. The third post in the series goes one level deeper: when an <code class="language-plaintext highlighter-rouge">Agent</code> tool call produces a separate subagent trace file, what’s in that file, how it relates back to the <code class="language-plaintext highlighter-rouge">toolUseResult.agentId</code> you just saw, and why subagent trace files are arguably the most structurally interesting part of the entire format.</p> <p>If you want to look ahead, <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/subagent-traces.md"><code class="language-plaintext highlighter-rouge">reference/subagent-traces.md</code></a> in the repo is where that work is grounded. Part 3 is the narrative on top.</p> <p>The synthetic fixtures referenced throughout this post are in <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/tree/main/fixtures/synthetic"><code class="language-plaintext highlighter-rouge">fixtures/synthetic/</code></a>. If you want to run the <code class="language-plaintext highlighter-rouge">jq</code> snippets without using a real session, those files are valid JSONL by design and contain the structural patterns described here.</p> <p>If there’s a line type or a behavior I didn’t cover that you’ve seen in your own sessions, I’d love to know — that’s exactly the kind of thing that updates the reference docs and refines later posts.</p> <hr/> <p><em>Drafted with Claude Code (verified against v2.1.150). The ideas, claims, and any errors are mine.</em></p>]]></content><author><name></name></author><category term="foundation"/><category term="claude-code"/><category term="agent-sdk"/><category term="jsonl"/><category term="sessions"/><summary type="html"><![CDATA[Part 2 of the anatomy series. Every type of line in a session JSONL, the snake_case/camelCase split that reveals two layers (API content wrapped in Claude Code's bookkeeping), and why tool results live inside user messages.]]></summary></entry><entry><title type="html">Anatomy of a Claude Code session</title><link href="https://frederick-douglas-pearce.github.io/blog/2026/anatomy-of-a-claude-code-session/" rel="alternate" type="text/html" title="Anatomy of a Claude Code session"/><published>2026-05-26T08:00:00+00:00</published><updated>2026-05-26T08:00:00+00:00</updated><id>https://frederick-douglas-pearce.github.io/blog/2026/anatomy-of-a-claude-code-session</id><content type="html" xml:base="https://frederick-douglas-pearce.github.io/blog/2026/anatomy-of-a-claude-code-session/"><![CDATA[<p>Every time you run Claude Code or invoke the Agent SDK, it writes a detailed record of your session to a file on your local machine. Every prompt you’ve typed. Every file the model has read or edited. Every tool it has called — and the result that came back. All of it lands in JSONL form at <code class="language-plaintext highlighter-rouge">~/.claude/projects/</code>, one file per session, sitting there quietly.</p> <p>Most people have never opened one of these files. That’s a shame, because understanding what’s in them changes how you think about every feature Claude Code offers: why <code class="language-plaintext highlighter-rouge">--continue</code> works, how <code class="language-plaintext highlighter-rouge">/rewind</code> can undo not just words but the actual files on your disk, where your token costs really come from, and what becomes possible when you treat this data as a first-class artifact rather than a byproduct.</p> <p>This is the first post in a series on that format. Part 1 covers the what and the why — where the files live, what shape they take, and what Claude Code itself uses them for. Part 2 will go field-by-field through the message-type taxonomy and walk through the more surprising structural details.</p> <hr/> <h2 id="where-it-lives">Where it lives</h2> <p>Sessions are stored at:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.claude/projects/&lt;slug&gt;/&lt;session-uuid&gt;.jsonl
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">&lt;slug&gt;</code> is derived from your working directory path, with slashes replaced by dashes. If you’re working in <code class="language-plaintext highlighter-rouge">/home/fred/myproject</code>, the slug is <code class="language-plaintext highlighter-rouge">-home-fred-myproject</code>. One project directory, one subdirectory.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.claude/projects/
└── -home-fred-myproject/
    ├── abc12345-wild-flow-0000-000000000001.jsonl
    ├── abc12345-wild-flow-0000-000000000001/
    │   └── subagents/
    │       └── agent-99999999-...jsonl
    └── abc12345-wild-flow-0000-000000000002.jsonl
</code></pre></div></div> <p>Each <code class="language-plaintext highlighter-rouge">.jsonl</code> file is one session. The matching subdirectory, if present, holds subagent trace files: one per agent invocation. Those traces are the subject of a later post. For now, it’s enough to know they exist.</p> <p>JSONL stands for JSON Lines — a format where each line is an independent, fully-formed JSON object. A session file has one JSON object per message or event. This makes sessions trivially streamable and parseable line by line without loading the entire file into memory.</p> <hr/> <h2 id="whats-recorded--in-plain-english">What’s recorded — in plain English</h2> <p>The short answer is: everything.</p> <p>Every prompt you type is in there. Every response the model generates. Every tool call — “read this file,” “write that function,” “run this shell command” — and every result that came back, including file contents, command output, and error messages.</p> <p>Token counts are on every assistant message — the raw data for cost analysis, though turning those numbers into accurate dollar figures takes more than a simple sum (more on that below). Timestamps are on every line. The working directory and Claude Code version are recorded per message (they can change mid-session if you switch directories or update the tool).</p> <p>For subagent sessions — when Claude delegates work to a specialized agent — the parent file records how long the subagent ran, how many tokens it used, how many tool calls it made, and a summary of the result. The subagent’s own step-by-step trace lives in its own file.</p> <p>A reader who finishes a session JSONL can reconstruct the entire interaction: what you asked, what the model decided to do, what it found, what it changed, and at what cost. That completeness is what makes the file useful for everything described below.</p> <hr/> <h2 id="what-this-powers-in-claude-code-itself">What this powers in Claude Code itself</h2> <p>This section is the behind-the-curtain reveal. Each of these features that you may use daily is powered, directly or indirectly, by reads against your session JSONL files.</p> <h3 id="resume-where-you-left-off">Resume where you left off</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>claude <span class="nt">--continue</span>      <span class="c"># resumes the most recent session in the current directory</span>
claude <span class="nt">-c</span>              <span class="c"># shorthand</span>
</code></pre></div></div> <p>When you run <code class="language-plaintext highlighter-rouge">--continue</code>, Claude Code finds the most recent <code class="language-plaintext highlighter-rouge">.jsonl</code> file in the current project’s subdirectory, reads it, and rebuilds the conversation context. The continuity you experience isn’t magic, it’s a file read.</p> <h3 id="browse-and-jump-between-sessions">Browse and jump between sessions</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>claude <span class="nt">--resume</span>        <span class="c"># opens the interactive picker</span>
/resume                <span class="c"># same thing from inside an active session</span>
</code></pre></div></div> <p>The session picker enumerates every <code class="language-plaintext highlighter-rouge">.jsonl</code> file in your project directory (and, if you press <code class="language-plaintext highlighter-rouge">Ctrl+A</code>, across all projects on your machine). Each row in the picker — the session name, message count, git branch, time since last activity — is derived from reading those files. When you select an entry, Claude Code loads that file as your new context.</p> <p>You can also filter by git branch (<code class="language-plaintext highlighter-rouge">Ctrl+B</code>) or search by pasting a pull request URL directly into the picker, and it will find the session associated with that PR.</p> <h3 id="branch-a-conversation">Branch a conversation</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/branch try-different-approach
claude <span class="nt">--continue</span> <span class="nt">--fork-session</span>
</code></pre></div></div> <p>Branching copies the session JSONL file up to the current point into a new file with a new session UUID. You’re now working in the copy; the original is untouched and remains in the picker under the same name. Two session IDs, two files, both resumable. The new branch shows up as a child of the original in the picker’s tree.</p> <p>This is “parallel universe territory”, but for code reviews, refactors, and anything else where you might want to recover the road not taken.</p> <h3 id="rewind-code-and-conversation">Rewind code and conversation</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/rewind                <span class="c"># or press Esc twice when the input is empty</span>
</code></pre></div></div> <p>This is the feature that most surprised me once I understood what was actually powering it.</p> <p>Every prompt you’ve sent in the session is a checkpoint boundary. The rewind menu lists them, and you pick one to act on. Then you choose what to restore: code and conversation together, conversation only (keep your current files), code only (keep the conversation), or compress part of the history into a summary instead of restoring at all.</p> <p>You can roll back not just what the model said, but the actual files it edited on your disk.</p> <p>What makes the “restore code” part possible? Those <code class="language-plaintext highlighter-rouge">file-history-snapshot</code> lines you’ll see in a session JSONL — entries that most analytics parsers skip as metadata — are precisely the data rewind needs: snapshots of file state at checkpoint moments. The docs don’t spell out the storage mechanism explicitly, but the fact that checkpoints “persist across sessions” and are “cleaned up along with sessions” makes the connection a near-certainty.</p> <p>The conversation history and the file history live in the same transcript, which is why you can restore them in combination or independently. Checkpoints persist across sessions, so you can resume a session days later and still rewind to an earlier point. They’re automatically cleaned up on the same schedule as the session files themselves.</p> <p>One caveat worth knowing: rewind covers files Claude edited directly through its file-editing tools. Files modified by bash commands (<code class="language-plaintext highlighter-rouge">rm</code>, <code class="language-plaintext highlighter-rouge">mv</code>, <code class="language-plaintext highlighter-rouge">cp</code>) or by you outside of Claude Code aren’t tracked. Think of it as session-level undo for Claude’s direct edits, not a replacement for git.</p> <h3 id="link-a-session-to-a-pull-request">Link a session to a pull request</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>claude <span class="nt">--from-pr</span> 42
</code></pre></div></div> <p>Session files can carry per-session PR association metadata. Once a session is linked to a PR number, you can resume it by PR number directly, and the session picker lets you paste a PR URL to filter to it. If you typically open a Claude Code session per feature branch, this gives you a stable handle back to the conversation that produced the PR.</p> <h3 id="export-and-share">Export and share</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/export
/export my-session-notes.txt
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">/export</code> reads the current session’s JSONL and renders it as plain readable text — prompts, responses, and tool outputs formatted for human consumption. Without a filename argument it copies to clipboard; with one it writes to disk. The underlying session file is untouched.</p> <h3 id="compact-long-sessions">Compact long sessions</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/compact
/compact focus on the auth module changes
</code></pre></div></div> <p>When context grows long, <code class="language-plaintext highlighter-rouge">/compact</code> replaces the earlier conversation history with an AI-generated summary. The summary becomes a new line in the JSONL — one that future resumes can load without replaying every prior exchange. The original messages are preserved in the transcript; the compacted summary is what gets loaded into the context window going forward.</p> <p>The <code class="language-plaintext highlighter-rouge">/rewind</code> menu offers targeted versions of the same idea: “Summarize from here” and “Summarize up to here” let you compress a specific slice of the conversation rather than everything before the current point.</p> <hr/> <h2 id="what-this-enables-for-you">What this enables for you</h2> <p>The most immediate thing you can do with this knowledge is direct inspection. Find your project’s JSONL directory:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ls</span> ~/.claude/projects/-home-you-yourproject/
</code></pre></div></div> <p>Stream a session as it’s being written:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">tail</span> <span class="nt">-f</span> ~/.claude/projects/-home-you-yourproject/&lt;session-uuid&gt;.jsonl
</code></pre></div></div> <p>Filter for just the token usage lines to total your costs:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> &lt;session&gt;.jsonl | jq <span class="s1">'select(.type=="assistant") | .message.usage'</span>
</code></pre></div></div> <p>(<code class="language-plaintext highlighter-rouge">jq</code> is a command-line JSON processor. If you don’t have it, <code class="language-plaintext highlighter-rouge">brew install jq</code> or <code class="language-plaintext highlighter-rouge">apt-get install jq</code> will get you there.)</p> <p>Note that summing <code class="language-plaintext highlighter-rouge">input_tokens + output_tokens</code> gives you raw usage, not actual cost. Pricing varies by model (Sonnet, Opus, and Haiku are each priced differently), cache reads (<code class="language-plaintext highlighter-rouge">cache_read_input_tokens</code>) are billed at a fraction of regular input tokens, cache creation (<code class="language-plaintext highlighter-rouge">cache_creation_input_tokens</code>) is billed at a premium, and subagent token totals are also reported in the parent session via the <code class="language-plaintext highlighter-rouge">toolUseResult</code> envelope — so naive aggregation can double-count. The next paragraph mentions tooling that handles this properly.</p> <p>You can also see which tools Claude called most often, how long each subagent took, where the model stopped and waited for tool results — all directly from the file.</p> <p>Two projects I’m building take this further: <a href="https://github.com/frederick-douglas-pearce/agentfluent">AgentFluent</a> diagnoses agent effectiveness from session traces — tool call patterns, retry rates, delegation efficiency. <a href="https://github.com/frederick-douglas-pearce/codefluent">CodeFluent</a> reads the same files from the human-AI interaction perspective, treating the patterns as a signal for AI fluency development. Both start from the same JSONL source; what they measure differs.</p> <hr/> <h2 id="a-peek-at-the-raw-shape">A peek at the raw shape</h2> <p>Here’s the complete content of <code class="language-plaintext highlighter-rouge">fixtures/synthetic/anatomy-minimal-session.jsonl</code> — the simplest possible session, two lines. Real JSONL is one JSON object per line with no internal line breaks; I’ve expanded them here for readability:</p> <pre><code class="language-jsonl">// from https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-minimal-session.jsonl
{
  "type": "user",
  "sessionId": "00000000-0000-0000-0000-000000000001",
  "uuid": "11111111-1111-1111-1111-111111111001",
  "parentUuid": null,
  "isSidechain": false,
  "cwd": "/home/dev/example-project",
  "version": "2.1.150",
  "timestamp": "2026-05-20T14:30:00.000Z",
  "message": {
    "role": "user",
    "content": "What's the capital of France?"
  }
}
{
  "type": "assistant",
  "sessionId": "00000000-0000-0000-0000-000000000001",
  "uuid": "22222222-2222-2222-2222-222222222001",
  "parentUuid": "11111111-1111-1111-1111-111111111001",
  "timestamp": "2026-05-20T14:30:01.342Z",
  "message": {
    "role": "assistant",
    "model": "claude-sonnet-4-6",
    "content": [{"type": "text", "text": "The capital of France is Paris."}],
    "usage": {
      "input_tokens": 12,
      "output_tokens": 8,
      "cache_creation_input_tokens": 0,
      "cache_read_input_tokens": 0
    },
    "stop_reason": "end_turn"
  }
}
</code></pre> <p>A few things to notice: <code class="language-plaintext highlighter-rouge">type</code> tells you what kind of line this is — <code class="language-plaintext highlighter-rouge">user</code> here, <code class="language-plaintext highlighter-rouge">assistant</code> on the next line. <code class="language-plaintext highlighter-rouge">sessionId</code> is shared across every line in the file; it’s the key that ties a session together. <code class="language-plaintext highlighter-rouge">parentUuid</code> on the first line is <code class="language-plaintext highlighter-rouge">null</code> because there’s no prior message; on the assistant reply it points back to the first line’s <code class="language-plaintext highlighter-rouge">uuid</code>. <code class="language-plaintext highlighter-rouge">timestamp</code> is UTC ISO 8601. And <code class="language-plaintext highlighter-rouge">cwd</code> is recorded on every line, as it can change within a session.</p> <p>The assistant reply adds <code class="language-plaintext highlighter-rouge">message.usage</code> with input and output token counts. The user message doesn’t carry usage because token accounting belongs to model responses.</p> <p>For the next level of complexity — a tool call and its result — see <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-tool-use-cycle.jsonl"><code class="language-plaintext highlighter-rouge">anatomy-tool-use-cycle.jsonl</code></a>. For what an Agent tool invocation looks like in the parent session, including the <code class="language-plaintext highlighter-rouge">toolUseResult</code> envelope that sits outside <code class="language-plaintext highlighter-rouge">message</code>, see <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/fixtures/synthetic/anatomy-agent-invocation.jsonl"><code class="language-plaintext highlighter-rouge">anatomy-agent-invocation.jsonl</code></a>.</p> <p>Those fixtures are where Part 2 starts. See <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/data-dictionary.md#message-types">Data Dictionary: message types</a> (forthcoming) for the full field-level reference.</p> <hr/> <h2 id="what-this-means-for-your-data">What this means for your data</h2> <p>The first thing worth saying: this data stays on your machine. Claude Code doesn’t automatically upload session transcripts anywhere. They’re local files in <code class="language-plaintext highlighter-rouge">~/.claude/projects/</code>, readable only by you.</p> <p>The second thing worth saying: they contain a lot. Every prompt you’ve typed, including ones where you pasted sensitive context, file contents you asked the model to review, command output including anything printed to stdout, and (if you’re not careful) any secrets that ended up in a tool result. The file reads back like a complete record of the working session — because it is.</p> <p>Practical guidance: think before sharing these files. If you want to share a session as an example — for a blog post, a bug report, or a team review — scrub it first. I’m building <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/tree/main/tooling/sanitizer"><code class="language-plaintext highlighter-rouge">tooling/sanitizer/</code></a> in <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions">the claude-code-sessions repo</a> specifically for this: a CLI that strips paths, identifiers, and secret patterns from session JSONL before you publish it. It’s not done yet, but the design is in progress.</p> <p>On retention: Claude Code’s documentation describes a 30-day default cleanup via the <code class="language-plaintext highlighter-rouge">cleanupPeriodDays</code> setting in <code class="language-plaintext highlighter-rouge">~/.claude/settings.json</code>. In practice, I’ve found older transcripts tend to persist well past 30 days on my own machine — at the time of this post’s publication, 45% of my 556 session files are older than the documented default. Whether this reflects a change in enforcement, a threshold condition for cleanup triggering, or something else, I can’t say with certainty.</p> <p>What I can say: if long-term retention matters to you — for analysis, regression checks, or feeding tools like AgentFluent and CodeFluent that work better with history depth — set <code class="language-plaintext highlighter-rouge">cleanupPeriodDays</code> explicitly rather than relying on the default behavior. I have mine set to 3650 (ten years — effectively never):</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"cleanupPeriodDays"</span><span class="p">:</span><span class="w"> </span><span class="mi">3650</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>Configure for your own retention preferences, not for what you hope the default will do.</p> <hr/> <h2 id="whats-next">What’s next</h2> <p>You’ve been writing JSONL session files every time you’ve used Claude Code. Now you know where they are, what’s in them at a high level, and why every major session management feature you use is reading them directly.</p> <p>This is Part 1 of an ongoing series. Part 2 goes field by field: the full message-type taxonomy (<code class="language-plaintext highlighter-rouge">assistant</code>, <code class="language-plaintext highlighter-rouge">user</code>, the types most parsers skip entirely), why tool results live inside <code class="language-plaintext highlighter-rouge">user</code> messages rather than as their own type, the <code class="language-plaintext highlighter-rouge">toolUseResult</code> envelope on Agent invocations and why it uses camelCase when nothing else does, and how subagent traces relate to parent sessions.</p> <p>If you want to get into the field-level detail before Part 2 lands, the <a href="https://github.com/frederick-douglas-pearce/claude-code-sessions/blob/main/reference/data-dictionary.md">reference documentation at <code class="language-plaintext highlighter-rouge">reference/data-dictionary.md</code></a> is being populated alongside this post series. It’s the canonical home for format documentation as this repo matures.</p> <p>Let’s connect if this resonates.</p> <hr/> <p><em>Drafted with Claude Code (verified against v2.1.150). The ideas, claims, and any errors are mine.</em></p>]]></content><author><name></name></author><category term="foundation"/><category term="claude-code"/><category term="agent-sdk"/><category term="jsonl"/><category term="sessions"/><summary type="html"><![CDATA[Every Claude Code session writes a detailed local JSONL record of your prompts, tool calls, and token usage — here's what's in it and what it powers.]]></summary></entry></feed>